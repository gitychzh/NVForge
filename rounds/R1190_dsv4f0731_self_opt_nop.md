# R1190: dsv4f0731_nv40666 NOP — 30min 全绿 (SR=100%, 0错误, 0 fallback, 0 429)

日期: 2026-08-08 08:52 UTC 采集窗口 (~30min)
容器: `dsvf0731_nv40666` (端口 40666, num_keys=5, DSV4F0731 NVCF 链路)

## 决策：NOP（不改任何参数）

连续第二十一轮全绿（R1170~R1190 均 SR=100%），健康稳态，守"改前必有数据"铁律不动作。

## 30min 窗口数据（脚本注入）

| 指标 | 值 |
|---|---|
| SR | **100%** (163/163, 0 error) |
| 错误 / Fallback | **0 / 0** |
| Avg / P50 / P95 | 11264 / 8166 / 40725 ms |
| 429 | 0 |
| upstream_type | nvcf_pexec 163/163 (100%) — 全 pexec，无 integrate |
| finish_reason | tool_calls 137, stop 26 |

**Per-key 200 延迟**（全 5 key 无错误，健康）:
```
key0|33req|avg12084|P9528483
key1|32req|avg11285|P9526725
key2|33req|avg8992|P9523075
key3|34req|avg13734|P9532711
key4|31req|avg10078|P9522839
```
5 key 负载均匀 (31-34 req/key)、延迟同量级 (9.0-13.7s avg)，k2 略快但无错误聚集，无劣化 key。per-key 错误为空——30min 内 **0 错误**。

**tier_attempts**: 空（30min 内 0 错误，无 key 切换失败）。

**key_cycle_429s**: 0|63, 1|100 — 与 R1189 (0|74, 1|94) 属噪声波动，30min 429=0 验明无 429/循环压力。

## 趋势

- **6h**: 2016/2011 → **SR=99.75%**，5 失败，0 fallback（5 失败均落在 30min 窗口之外，为长期稀疏速率，非本窗口信号）
- **3h 逐小时**: 00:00 281(100%), 23:00 316(100%), 22:00 265(100%*), 21:00 48(100%) — 高流量且逐小时 SR 全 ≥98.9%（22:00 3 error 为更早窗口已确认的稀疏事件）
- **24h all_tiers_exhausted**: 44（~1.8/h，均被 fallback 兜住，非本 30min 窗口信号，与 R1189/R1188 持平）

## 关键判断

- 30min SR=100%、0 错误、0 fallback、0 429 — **连续第二十一轮全绿**（R1170~R1190 均 100%，窗口请求量 31-316、延迟 avg 稳定在 9.0-13.7s 带）。
- 全部 pexec、无 key 劣化、per-key 分布均匀 (31-34/key)、avg 9.0-13.7s 稳定。key_cycle_429s 略有噪声波动但 429=0，无压力。
- 6h SR=99.75% + 逐小时高流量（最高 316/h）稳定 SR ≥98.9% → 整体链路完全健康稳态，无任何退化信号。
- P95 带 22.8-32.7s 为 NVCF pexec 正常稀疏尾部，SR 仍 100%（P95 波动属噪声，R1189 为 16.5-36.5s，量级一致）。

**无任何调参依据。** 守"改前必有数据"铁律——健康窗口不动作，避免为调而调。

## 上次修改效果 (R1189 → R1190)

R1189 SR=100% (168/168)。本轮 SR=**100%** (163/163)，0 错误，连续全绿再次验证 NOP 判断正确。per-key 延迟带 8.2-12.6s → 9.0-13.7s（同量级稳定，噪声内）。key_cycle_429s 噪声波动（k0 74→63, k1 94→100）但 429=0 无实际压力。**无需要修的参数。**

## 下一步建议

- 保持观察。连续二十一轮全绿强 NOP 信号，下轮若仍无 429 回升 / 错误聚集，继续 NOP。
- 预置对策（仅当信号出现才动作，沿用之前设定）:
  - **429 回升 ≥5/30min** → `KEY_COOLDOWN_S` 30→60s。
  - **all_tiers_exhausted ≥3/30min 且单 key 聚集** → 检查该 key 出口 IP / 代理质量，非直接改参。
  - **NVCF 错误聚集**（同种 ≥3/30min） → 排查上游出口而非盲目调超时（单次事件已被 key 循环兜住，不足为凭）。
- 容器 6h /health OK、全 5 key 正常、no integrate 激活—保持现有配置不变。