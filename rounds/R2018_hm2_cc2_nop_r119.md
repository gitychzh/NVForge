# R2018 (HM2 cc2): NOP 巡检 R119 — 30min SR96.15%/6h SR96.32% 0 真中断, vs R2016 6h +0.00pp 区间内持平非退化, 连续冻结第 55 轮延续

> 铁律: 只改 HM2 nv_gw(40006), 不碰 HM1 / ms_gw(40007). 本轮 0 改动 0 restart.

## 数据 (本 session 拉取, nv_gw StartedAt 13:33:43Z 维 R1933, 0 restart)

### 30min 窗口
- nv_gw 30min SR = 50/52 = **96.15%** (200:50 / 502:2)
- 30min 502=2 全 zombie_empty_completion (已知类, 与 R2016 30min 502=2 **完全同构**)
- tier 30min error_type: pexec_success×45 / pexec_conn_RemoteDisconnected×3 / pexec_429×2 / pexec_500×2 / pexec_empty_200×1
  (pexec_500×2 是上游 NVCF 500 抖动非 nv_gw 可配置类; pexec_empty_200×1 也是上游空响应. 已知上游抖动, 不需动.
  vs R2016 pexec_success 46→45 (-1), pexec_429 2→2 持平, pexec_conn 3→3 持平 — 上游 NVCF 抖动小样本波动)

### 6h 窗口
- nv_gw 6h SR = 995/1033 = **96.32%** (200:995 / 502:38)
- 6h 502=38 全已知三类 (与 R2016 6h 502=38 **完全持平, 0 重新分布**):
  zombie_empty_completion×24 / stream_first_byte_timeout×8 / all_tiers_exhausted×6. **0 新可配置类**.

### abs_cap
- 30min=0 / 6h=0 (DB `error_type like '%abs%'` 0 rows 双重确认; R1918 方案0 cap_origin 重置持续归零)

### fallback (cc4101 30min)
- fallback **9** FALLBACK-OK (0 真中断, 0 fallback 失败): 全 9 条 75s `PRIMARY-FAIL-SKIP-CIRCUIT`
  (< chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层).
- 120s 跑满类本轮 0 持续趋稳归零 (全部 fallback 都在 75s SKIP 层被兜住, 无任何跑满 120s chain budget).
- 全 9 条被 cc4101 在 75s 抢断切 ms, ms 救回 → **0 条 fallback 失败 → CC 收 0 真 502**.
- `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` cc4101 30min = 0 → 确认 0 真中断.

### breaker
- cc4101 PRIMARY-BREAKER-OPEN 30min = **0**
- nv_gw 30min `grep -cE "BREAKER-FAIL|BREAKER.*OPEN|NV-ANTH-BREAKER-FAIL"` = **0**
  (R2015=0 → R2016=0 → 本轮 0, 连续 4 轮无 recorded 事件, 偏好非恶化)

### BUG-A (R1913 修复) 在日志中真实生效确认
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **6 次** (与 R2016 相同, 区间内波动持续触发确认机制长期生效)

## 决策: NOP (0 改动 0 restart)

数据全部满足"稳态":
- 30min SR 96.15% (>94% 阈值), 6h SR 96.32% (>95% 阈值, 区间内 94.0-96.5% 非退化)
- 502 全 NVCF 上游已知三类 (zombie / stream_first_byte_timeout / all_tiers_exhausted), 0 新可配置类
- abs_cap 30min/6h 归零 (R1918 方案0 持续生效)
- fallback 全 75s SKIP 被兜住, 0 fallback 失败 0 真中断
- breaker 未 OPEN (nv_gw + cc4101 双侧 0 recorded 事件)
- BUG-A SKIP-PEXEC2 持续触发 (R1913 修复真实生效)

冻结理由 (连续第 55 轮) 仍成立: 半成品未 in-vivo 验证 (env NVU_GLM52_EXP_BACKOFF 从未激活, 根本不在容器 env 中)
+ 激活需同步 chain_budget 120→420 + cc4101 header 60→450 + post-200 软挂换 key 未实现 + 24h 观测窗口.
风险/收益不对等 (当前 6h SR96.32% 0 真中断, abs_cap 连续多轮归零, BUG-A 修复真实生效, 120s 跑满类持续趋零, 边际收益小).

## 验证
- 0 改动, 无需 restart. `curl /health` = ok (passthrough, 5 keys).
- nv_gw StartedAt = 2026-07-19T13:33:43Z (R1933→R2018 未再 restart, docker inspect 核实).
- cc4101 StartedAt = 2026-07-19T12:10:22Z (R1926 以来 0 restart).

## 参数快照 (docker exec env, 与 R2016 完全一致)
```
KEY_COOLDOWN_S=25
KEY_AUTHFAIL_COOLDOWN_S=60
NVU_BIG_INPUT_FAIL_N=1
UPSTREAM_TIMEOUT=66
TIER_TIMEOUT_BUDGET_S=180
NV_INTEGRATE_KEY_COOLDOWN_S=90
TIER_COOLDOWN_S=25
NVU_BIG_INPUT_COOLDOWN_S=180
MIN_OUTBOUND_INTERVAL_S=0
```
(NVU_GLM52_EXP_BACKOFF 不在 env 中 = 关, 半成品冻结中. chain_budget 仍 120s, 未升 420.)

## 轮号说明
- peer HM1 已用 hm2_optimize_hm1 前缀取走 R2017 (commit 4cea1aa4, 改 HM1 KEY/TIER_COOLDOWN 62→60, 不碰 HM2).
- cc2 用 R2018 hm2_cc2 前缀避撞号, 同号不同前缀无冲突.
