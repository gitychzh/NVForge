# R2007 (HM2 cc2) — NOP 巡检 R112 (连续第 48 轮冻结)

> 链路: cc2 → cc4101(127.0.0.1:4101) → nv_gw(40006, glm5_2_nv) → NVCF
>   ↘ ms_gw(40007, glm5_2_ms) [兜底热备, 不碰]

## 数据 (改前必有数据 — 本 session 拉取, nv_gw StartedAt 2026-07-19T13:33:43Z 维 R1933)

### 30min 窗口
- nv_gw 30min SR = 99/104 = **95.19%** (200:99 / 502:5), 区间内
  (R2005 95.93 / R2006 95.54 / 本轮 95.19, 30min 小样本波动 -0.35pp 非退化)
- 30min 502=5 全 NVCF 上游侧已知类:
  - stream_first_byte_timeout ×3 (上游 TTFB 超时)
  - all_tiers_exhausted ×2 (全 tier 耗尽)
  - 与 R2006 30min 502=5 (sfbt×3/ate×2) **完全一致, 无恶化**
- tier 30min error_type:
  - pexec_success ×63 / pexec_conn_RemoteDisconnected ×5 / pexec_SSLEOFError ×1
  - (与 R2006 pexec_success×67/RemoteDisconnected×5/429×3/SSLEOFError×1 基本一致;
    本轮 0 pexec_429 / RemoteDisconnected 持平 / SSLEOFError 持平, 小样本波动非新可配置类)

### 6h 窗口
- nv_gw 6h SR = 986/1025 = **96.20%** (200:986 / 502:39), 大样本稳态区间
  (R1960-2007 94.0-96.5% 区间内非退化, **vs R2006 6h 96.09% +0.11pp 区间内偏升非退化**)
- 6h 502=39 全已知三类 (与 R2006 一致, R2006 为 40 → 39 微降):
  - zombie_empty_completion ×24
  - stream_first_byte_timeout ×9
  - all_tiers_exhausted ×6
  - **0 新可配置类 0 重新分布**
- abs_cap 30min=0 / 6h=0 (DB like '%abs%' 0 rows; R1918 方案0 cap_origin 重置持续归零,
  连续多轮: R2004=0 → R2005=0 → R2006=0 → R2007=0)

### fallback / breaker (负向核心指标)
- fallback **6** FALLBACK-OK (0 真中断, 0 fallback 失败):
  全 6 条 75s `PRIMARY-FAIL-SKIP-CIRCUIT` (< chain budget 120s, cc4101 pre-empted nv_gw
  retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层)。
  R2005 4 / R2006 6 / 本轮 6 (区间内波动, 类别仍是已知类)。
  **120s 跑满类本轮 0 持续趋稳归零** (全部 fallback 都在 75s SKIP 层被兜住, 无跑满 120s)。
  全 6 条被 cc4101 在 75s 抢断切 ms, ms 8858/4132/3183/1527ms 救回 → 0 fallback 失败 → CC 收 0 真 502。
  `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` cc4101 30min = 0 → 确认 0 真中断。
- breaker cc4101 PRIMARY-BREAKER-OPEN 30min = **0**;
  nv_gw 30min `grep -cE "BREAKER-FAIL|BREAKER.*OPEN"` = **0** — breaker OPEN 0 连续多轮。

### BUG-A 修复 (R1913) 真实生效确认
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **5 次**
  (R2005 3 / R2004 3 / R2003 3 / R2002 2 / R2006 4 / 本轮 5, 区间内波动持续触发确认机制长期生效)

## 拟改 / 预期
- **0 改动 0 restart** (NOP 巡检)。数据全部满足稳态: 30min SR>94%, 6h SR>95% (96.20),
  502 全 NVCF 上游已知三类, abs_cap 归零, fallback 全兜住 0 真中断, breaker 0 OPEN, BUG-A 持续触发。
- 冻结理由 (连续第 48 轮) 仍成立: 半成品未经 in-vivo 验证
  (NVU_GLM52_EXP_BACKOFF 不在 env 中从未激活) + 激活需同步 chain_budget 120→420
  + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 + post-200 软挂换 key 未实现 + 24h 观测窗口。
  风险/收益不对等 (当前 6h SR96.20% 0 真中断, abs_cap 连续多轮归零, 120s 跑满类持续趋零)。

## 验证清单
- [x] curl /health ok (proxy_role=passthrough, nv_num_keys=5, port=40006)
- [x] docker ps: nv_gw Up / cc4101 Up / ms_gw Up / logs_db Up
- [x] nv_gw StartedAt 13:33:43Z 维 R1933 未变 (本轮 0 restart 确认)
- [x] env 参数快照全部与 R2006 一致 (本轮 0 改动)

## 当前 nv_gw 参数快照 (docker exec env)
```
KEY_COOLDOWN_S=25
KEY_AUTHFAIL_COOLDOWN_S=60
NVU_BIG_INPUT_FAIL_N=1
UPSTREAM_TIMEOUT=66
TIER_TIMEOUT_BUDGET_S=180
NV_INTEGRATE_KEY_COOLDOWN_S=90
TIER_COOLDOWN_S=25
NVU_BIG_INPUT_COOLDOWN_S=180
MIN_OUTBOUND_INTERVAL_S=0
```
(NVU_GLM52_EXP_BACKOFF 不在 env 中 = 关, 半成品冻结中. chain_budget 仍 120s, 未升 420.
本轮 0 改动, 参数与 R2006 快照完全一致.)

## 结论
R2007 NOP R112, 连续第 48 轮冻结指数退避+ms 双层方案, 0 改动 0 restart。
vs R2006 6h +0.11pp 区间内偏升非退化。用户诉求 "可以报错但不能让 cc2 中断" 仍达成。
铁律全程遵守: 无 .bak (无改动), 未碰 ms_gw, 仅 HM2。
