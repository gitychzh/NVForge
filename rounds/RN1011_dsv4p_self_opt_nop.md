# RN1011: NOP — dsv4f0731_nv 链路健康 (SR=98.8%, 0 fallback), 不改参数

**日期**: 2026-08-08
**采集窗口**: 2026-08-08 ~02:24 UTC (基线; RN1009 改后第二验证窗口)
**容器**: dsvf0731_nv40666 (port 40666, DeepSeek V4 Pro via NVCF)
**主机**: HM2 (opc2sname)
**改动类型**: NOP (无修改)

## 当前参数 (实测 env 确认)

| 参数 | 当前值 | 设置轮次 |
|------|--------|---------|
| `UPSTREAM_TIMEOUT` | **50** | RN1009 |
| `KEY_COOLDOWN_S` | 30 | 默认 |
| `TIER_COOLDOWN_S` | 90 | R1007 |
| `TIER_TIMEOUT_BUDGET_S` | 180 | 默认 |
| `NVU_TIER_BUDGET_DSV4F0731_NV` | 180 | 默认 |
| `NVU_PEXEC_TIMEOUT_FASTBREAK` | 3 | 默认 |
| `NVU_EMPTY_200_FASTBREAK` | 3 | 默认 |
| `NV_KEY_INTEGRATE_KEYS` | (空) | R1006 |
| `NVU_KEYMGR_429_BASE/MAX_COOLDOWN` | 120/120 | 默认 |
| `NVU_KEYMGR_CONN_*` | 30/60/3/120 | 默认 |

所有 integrate 相关保持为空（R1006 清空），纯 pexec 路径。无环境变量漂移。

## 数据

### 30min 主指标
| 指标 | 值 |
|------|-----|
| 总量 | 173 |
| 成功 | 171 |
| 失败 | 2 |
| SR | **98.8%** |
| Avg/P50/P95 | 12674ms / 7151ms / 26665ms |
| Max(p99) | 119411ms |
| 429 总计数 | **0** |

### 30min 错误分布
| 错误类型 | 次数 | avg ms |
|----------|------|--------|
| all_tiers_exhausted | 2 | 246,927 |

### 30min per-key 200 延迟 (全部 5 key 健康)
| Key | 成功 | avg(ms) | p95(ms) |
|-----|------|---------|---------|
| k0  | 34   | 8,945   | 22,958 |
| k1  | 35   | 9,673   | 23,724 |
| k2  | 34   | 9,326   | 25,829 |
| k3  | 37   | 11,887  | 26,985 |
| k4  | 31   | 9,648   | 24,504 |

各 key 负载均衡（31-37 请求），无单 key 劣化、无尾部异常拉偏。

### 30min per-key 错误
| Key | 错误 |
|-----|------|
| —   | 无单 key 集中错误 |

2 次 ATE 为系统级（跨全 key 遍历后无 key 成功），非单 key 问题。

### upstream_type 分布
| 类型 | 请求 | 200 | avg(ms) |
|------|------|-----|---------|
| nvcf_pexec | 171 | 171 | 9,934 |
| integrate | 0 | — | — |

100% 成功走 pexec；integrate 完全闲置（R1006 效果持续）。2 次 failed 为 all_tiers_exhausted（upstream 未定型）。

### finish_reason
| 原因 | 次数 |
|------|------|
| tool_calls | 154 |
| stop | 17 |

正常分布，无空响应。

### key_cycle_429s 分布
| cycle=0 | cycle=1 |
|---------|---------|
| 99 | 74 |

多数请求零 429 或仅一次 429 即成功，无多次 429 死循环。（实际 429 计数=0，cycle 计数为历史片保留。）

### 趋势
| 窗口 | 总量 | 成功 | 失败 | SR | avg ms |
|------|------|------|------|----|--------|
| 30min | 173 | 171 | 2 | 98.8% | 12,674 |
| 6h | 1,749 | 1,736 | 13 | 99.3% | — |
| 24h ATE | — | — | 126 | — | — |

逐小时 SR (UTC):
- 15:00-16:00: 172/172 SR=100%, avg=11.1s
- 16:00-17:00: 273/273 SR=100%, avg=13.4s
- 17:00-18:00: 279/273 SR=97.8%, avg=11.8s
- 18:00-19:00(当前): 156/156 SR=100%, avg=10.2s

### HM4104 Fallback
最近 5 分钟：**0 条 fallback 记录**（RN1010 的瞬时 2 次未复现）。无 fallback 触发。

## RN1009 上次修改效果验证 (UPSTREAM_TIMEOUT 90→50), 连续两窗
| 指标 | RN1008 | RN1009改后RN1010 | RN1011 |
|------|--------|-----------------|--------|
| 30min SR | 97.1% | 98.4% | **98.8%** |
| 30min ATE | 4 | 3 | 2 |
| 30min fallback | — | 2 | **0** |
| p95 | 34.4s | 27.5s | 26.7s |
| 429 | 0 | 0 | 0 |

RN1009 的 UPSTREAM_TIMEOUT 50s 连续两窗验证：
- SR 连续上升 (97.1→98.4→98.8%)
- ATE 连续下降 (4→3→2)
- fallback 归零 (RN1011 0 触发)
- 无 RemoteDisconnected/IncompleteRead 反弹

### ATE 深度分析
2 次 ATE avg 247s > TIER_TIMEOUT_BUDGET=180s，且在 UPSTREAM_TIMEOUT=50 下 247s ≈ 5^key 全失败（含连接建立/重试开销）。为**真实全-key NVCF 故障型**，非预算耗尽型（RN1009 已消除预算耗尽型）。此类无法通过降超时解决，仅靠 TIER_COOLDOWN_S=90 (R1007) 在故障窗口避免重复命中。频率低 (1.2%)。

## 分析结论
**链路状态: 优秀**。30min SR=98.8%, 6h SR=99.3%, 当前小时 100%, 429=0, fallback=0, 5 key 负载均衡, integrate 归零, 无单 key 错误集中, 无冷却级联。

2 次 ATE (1.2%) 为真实全-key NVCF 瞬时故障，非参数可调范围，且频率低于判断阈值。

**不修改决定**: SR>95% 触发 NOP 规则。所有指标健康且 RN1009 改动已确认正向收敛，无需调整。

## 下一步建议
1. **持续观察 24h ATE**: 当前 126 含 RN1009 改前历史，需再累计 1-2 窗确认全失败型 ATE 是否稳定在 ≤1.5%。
2. **HM4104 fallback**: RN1011 已归零，继续观察是否保持 <10 次/h。
3. **无需参数调整**: 现配置 (UPSTREAM_TIMEOUT=50, TIER_COOLDOWN=90, BUDGET=180) 在 pexec-only 下连续两窗健康。