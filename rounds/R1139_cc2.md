# R1139 cc2 NOP 巡检轮

**类型**: NOP 巡检轮（不改码）
**时间**: 2026-08-08 ~01:10 CST
**容器**: nv_gw 26h / cc4101 21h 运行中

## 结论
30min 主链零表面错误。cc2-primary `200|106 = 100% SR`（0 行非-200）。
唯一 surface 错误 `NVStream_IncompleteRead 1×`（502）**归属 hermes 非 cc2**。
tier 非-success 5 次分布式单点 self-heal（RD 4× k0/k2/k4 分散 + empty_200 1× k2），
低频下沉稳态，与上轮形态一致，未上浮 surface。buffer 全 attempt-1 direct flush，
无 WAIT/无 exhaust。fallback 0%（109 行 0 触发）。cc2 范围无配置回归 → **不改码**。

## 依据（本 session 实查 2026-08-08 01:10）

### 30min cc4101-primary (nv_requests 实查)
```
 status | count
--------+-------
    200 |   106
```
**0 行非-200 → 100% SR，主链全绿。**

### 30min 非-200 归属 (实查)
```
 caller | status |       error_type        |          created_at
--------+--------+-------------------------+-------------------------------
 hermes |    502 | NVStream_IncompleteRead | 2026-08-07 17:08:46.702893+00
```
唯一非-200 **归属 hermes**（dsv4f0731_nv 线, hermes 侧 transient IncompleteRead），
非 cc2 主链。cc2 = 0 行非-200。

### 30min nv_tier_attempts 非-success per-key (实查)
```
 nv_key_idx |         error_type          | count
------------+-----------------------------+-------
          0 | NVCFPexecRemoteDisconnected |     1
          2 | NVCFPexecRemoteDisconnected |     1
          2 | empty_200                   |     1
          4 | NVCFPexecRemoteDisconnected |     2
```
RD 4×（k0/k2/k4 各散点）+ empty_200 1×（k2），共 5 次。
各 key/time 分散单点 self-heal，无同 key 连续复发，未上浮 surface。
低频下沉稳态，延续 [[ssleof-error-transient-egress-blip]] / [[k3-transient-execute-failed-self-heal]]。

### 30min buffer 日志 (实查, 尾部多为 attempt-1 direct flush)
全 attempt-1 direct flush：`success_tool_call` / `success_text`，elapsed 1~13s。
无 execute_failed、无 backoff、无 WAIT、无新 exhaust。链路健康。

### 30min fallback
cc_requests 30min 109 行，`fallback_triggered=0` → **0%，未触发 ms_gw**。

## 改动
无（NOP。主链全绿 + fallback 0% + tier 错误分布式 self-heal + buffer direct flush → 维持稳态）。

## 验证
- 主链 SR 100%（106/106），surface 错误归 hermes 非 cc2 → 无需改码。
- nv_gw / cc4101 /health 200（本次未重启，容器运行 26h/21h 稳定）。

## 下一步
- 保持 NOP 稳态。观察 tier RD/empty_200 是否持续分散（单点 self-heal）还是
  收敛于某 key（若 k4 连续多发 RD，查 mihomo 7899/gateway 9766 线路）。
- hermes 侧 IncompleteRead 归 hermes 线，非 cc2 范围，不处理。