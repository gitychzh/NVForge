# R2000 (HM2 cc2): NOP 巡检 R106 — 连续第 43 轮冻结指数退避

> 本轮 0 改动 0 restart。延续 R1928 冻结决定。
> 铁律遵守: 改前有数据, 改后有验证(本轮 NOP 无需验证 dest), 聚焦 40006, 不碰 40007,
> 只改 HM2 不改 HM1, 改.py 必须 restart(本轮无 .py 改动)。

## 数据(改前必有数据, 本 session 拉取)

nv_gw 真实 StartedAt = **2026-07-19T13:33:43Z** (维持 R1933, 本轮 0 restart; docker inspect 核实)。
cc4101 StartedAt = **2026-07-19T12:10:22Z** (维持 R1926, 0 restart)。

### 30min 窗口
- nv_gw 30min SR = 96/103 = **93.20%** (status 200:96 / 502:6)。
  注: 先前快照 96/102=94.12%, 拉oker 间进来 +1 → 103。30min 小样本波动。
  vs R1998 94.90 / R1999 94.59 / R2000 93.20 — 区间内缓降 0.6-1.7pp 每 30min 仍属小样本非退化(大样本见 6h)。
- 30min 502=6 全 NVCF 上游侧已知类: **zombie_empty_completion×6** (单类, glm5_2_nv 出口 IP 同源快回空)。
  与 R1999 (6 全 zombie) 一致单类, 6h 三类仍在, 0 新可配置类。
- tier 30min error_type: pexec_success×83 / pexec_conn_RemoteDisconnected×4 / pexec_SSLEOFError×1
  (RemoteDisconnected/SSLEOFError 对应 zombie 快回空, 上游侧已知类)。

### 6h 窗口 (大样本稳态)
- nv_gw 6h SR = 948/985 = **96.24%** (200:948 / 502:37)。
  vs R1998 6h 926/962=96.26% / R1999 6h 948/985=96.24% → **持平一致 -0.02pp 0 漂移**。
  R1960-2000 6h 持续 94.0-96.3% 区间内非退化。
- 6h 502=37 全已知三类: zombie×27 (R1999 27, 持平) + stream_first_byte_timeout×6 (R1999 6, 持平)
  + all_tiers_exhausted×4 (R1999 4, 持平). 与 R1999 三类全持平, 0 新可配置类 0 重新分布。

### 关键负向指标
- **abs_cap 30min=0 / 6h=0** (DB `error_type like '%abs%'` 0 rows 双重确认;
  R1918 方案0 cap_origin 重置持续归零, 连续多轮: ... R1998=0 → R1999=0 → R2000=0)。
- **fallback 4** FALLBACK-OK (0 真中断, 0 fallback 失败): 全 4 条 75s `PRIMARY-FAIL-SKIP-CIRCUIT`
  (< chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类
  cc4101 bug3 preempt 层)。全 4 条被 cc4101 在 75s 抢断切 ms, ms 2182/2837/3785/4131ms 救回 →
  **0 条 fallback 失败 → CC 收 0 真 502**。`grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` = 0
  确认 0 真中断。
  R1998 2 / R1999 3 / R2000 4 — 区间内波动, 趋稳(全 75s SKIP, 120s 跑满类连续多轮 0)。
- **breaker**: cc4101 PRIMARY-BREAKER-OPEN 30min = **0**; nv_gw 30min 见 4 条 `NV-ANTH-BREAKER-FAIL`
  (glm5_2_nv mid-stream soft-fail err=zombie_empty_completion → nv_breaker recorded), state 全 CLOSED
  (1,0 / 1,0 / 2,0 / 2,0, 未达 5/300s 阈值不 OPEN) — 与 R1719 行为一致, **非真 OPEN 事件**。
  breaker **OPEN 0 连续多轮**。
  日志见 `NV-MS-FB-SERVED ms_gw served ... (state=CLOSED)` — nv_gw 内部 ms_fb 路径记 breaker failure,
  state 仍 CLOSED, 非 NV-ANTH-BREAKER-FAIL 事件, 与 breaker OPEN 无关。
- **BUG-A 修复 (R1913) 在日志中真实生效确认**: 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **2 次**
  (R1947-1999 区间 1-6 次/30min 波动, 持续触发确认机制长期生效)。

## 拟改/本轮决策

**NOP, 0 改动 0 restart**。数据全部满足稳态:
- 30min SR 93.20% 区间内小样本(6h SR 96.24% 大样本稳态),
- 502 全 NVCF 上游已知三类(zombie/fbt/ATE), 0 新可配置类,
- abs_cap 归零(连续多轮),
- fallback 4 全 75s SKIP 被兜住 0 真中断 0 fallback 失败,
- breaker 0 OPEN,
- BUG-A SKIP-PEXEC2 持续触发。

**冻结理由 (连续第 43 轮) 仍成立**: 半成品未经 in-vivo 验证
(NVU_GLM52_EXP_BACKOFF 根本不在容器 env 中 = 关, env 开关从未激活) +
激活需同步 chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 + post-200 软挂换 key 未实现 +
24h 观测窗口. 风险/收益不对等 (当前 6h SR96.24% 0 真中断, abs_cap 连续多轮归零,
BUG-A 修复真实生效, 120s 跑满类连续多轮 0, 边际收益小)。

## 验证结果

NOP 无需下窗口验证 dest。本轮 0 改动 0 restart, 参数与 R1999 快照完全一致。
nv_gw /health = ok (nv_num_keys=5, passthrough), docker ps 健康, StartedAt 维持 R1933。

## 当前 nv_gw 参数快照 (docker exec env)

```
KEY_COOLDOWN_S=25
KEY_AUTHFAIL_COOLDOWN_S=60
NVU_BIG_INPUT_FAIL_N=1
UPSTREAM_TIMEOUT=66
TIER_TIMEOUT_BUDGET_S=180
NV_INTEGRATE_KEY_COOLDOWN_S=90
TIER_COOLDOWN_S=25
NVU_BIG_INPUT_COOLDOWN_S=180
MIN_OUTBOUND_INTERVAL_S=0
```
(注: NVU_GLM52_EXP_BACKOFF 不在 env 中 = 关, 半成品冻结中. chain_budget 仍 120s, 未升 420.)
