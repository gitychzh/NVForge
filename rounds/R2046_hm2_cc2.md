# R2046 (HM2 cc2): NOP 巡检 R140 — 连续第 76 轮冻结指数退避

> env NVU_GLM52_EXP_BACKOFF 从未激活 (不在容器 env 中). 0 改动 0 restart.
> 铁律: 只改 HM2 nv_gw(40006), 不碰 HM1, 不碰 ms_gw(40007, 重启窗口热备).

## 拉取数据时间点
本 session 拉取, 当前 CST 14:10 (2026-07-20). git pull up to date. 仓库最新:
- 主分支 HEAD = 04da476 R2044 (HM2 oc2) NOP 巡检轮 2
- cc2 上一轮 = R2045 (d553934 + dffdaae 补轮)
- peer 抢号: oc2 已到 R2044, hermes2 到 R2044 (dsv4p_nv 诊断). cc2 本轮用 R2046 hm2_cc2 前缀避撞号.

## 本轮关键事件: nv_gw 在 R2045 之后被外部重启 (非 cc2 改)

**docker events 13:57:58 CST 实锤**:
- 13:57:58.111 container kill (signal=15 SIGTERM) → stop → die (exitCode=143, 正常终止)
- 13:57:59.046 container start (新容器 ID ea43081e)
- config-hash: 0fd6cdbc → 18fb44dd (**变了**, 伴随 compose 配置)
- RestartCount=0, OOMKilled=false → **非崩溃非 OOM, 是外部主动 docker stop+start 或 compose up/restart**
- 13:36-13:46 期间有大量 `docker exec nv_gw env` + `python3 -c import config ... dsv4p_nv tier config` 的 probe 事件
  (某个 agent 在探查 dsv4p_nv 配置), 然后 13:57 重启

**peer oc2 R2044 (commit 04da476) 已记录此漂移**: "nv_gw StartedAt 漂移 03:02→05:57
(docker daemon 级容器重建, env 一致无退化, RestartCount=0)". oc2 结论 "env 一致无退化".

**cc2 本轮核实 env 实际值 (纠正 R2042 STATE 过时快照)**:
R2042 STATE 末尾 env 快照记 `UPSTREAM_TIMEOUT=66` 是**长期复制旧快照的过时值**, 实际当前生产:
```
UPSTREAM_TIMEOUT=90            # compose 第31行注释 "R1: 66→90"
NVU_TIER_BUDGET_GLM5_2_NV=120  # compose 第77行
NVU_TIER_BUDGET_DSV4P_NV=120  # compose 第78行注释 "R1: 70→120"
KEY_COOLDOWN_S=25
KEY_AUTHFAIL_COOLDOWN_S=60
NV_INTEGRATE_KEY_COOLDOWN_S=90
TIER_COOLDOWN_S=25
TIER_TIMEOUT_BUDGET_S=180
NVU_BIG_INPUT_FAIL_N=1
NVU_BIG_INPUT_COOLDOWN_S=180
NVU_BIG_INPUT_THRESHOLD=250000
NVU_BIG_INPUT_MODELS=glm5_2_nv
NVU_FORCE_STREAM_UPGRADE=0
NVU_FORCE_STREAM_UPGRADE_TIMEOUT=150
NV_GLM52_MODE_CHAIN=integrate_us_rr,pexec_us_rr
MIN_OUTBOUND_INTERVAL_S=0
```
注: NVU_GLM52_EXP_BACKOFF **仍不在 env 中 = 关**, 半成品冻结中. chain_budget 仍 120s, 未升 420.
(UPSTREAM_TIMEOUT 66→90 和 NVU_TIER_BUDGET_GLM5_2_NV 115→120 何时改的无法从 git 溯源:
compose 不在仓库, 仅 /opt/cc-infra/docker-compose.yml; compose 注释 "R1: 66→90"/"R1: 70→120"
表明是有意改动. 本轮 cc2 未改任何 env, 仅记录真实值纠正 STATE 过时快照.)

## 数据 (本 session 拉取, 当前 CST 14:10)

### 30min 窗口 (含 13:57 重启窗口期, 流量 111 req)
- nv_gw 30min status: 200×98 / 502×12 / 400×1 → SR = 98/111 = **88.29%**
  (400×1 BadRequest 是客户端错误非 nv_gw 故障, 扣除后 200/110 = 89.09%)
- 30min SR 低于巡检阈值 94% 但: (1) 流量 111 req 仍小样本; (2) **含 13:57 重启窗口期**,
  重启那条 req=23211ada PRIMARY-FAIL RemoteDisconnected (13.8s) 被 ms_gw FALLBACK-OK 兜住 (19.3s)
  — 这是 40007 热备的实战验证, 非退化; (3) 6h 大样本 95.07% 是真实稳态.
- 30min 502=12 分类: zombie_empty_completion×5 / stream_first_byte_timeout×4 / all_tiers_exhausted×3
  + BadRequest×1. **全 NVCF 上游已知三类, 0 新可配置类**.
- vs R2045 30min SR90.16% (55/61) → 本轮 88.29% -1.87pp, 区间内波动 (含重启窗口期属预期).
  vs R2042 30min SR91.04% (61/67) → -2.75pp, 区间内波动.

### 6h 窗口 (大样本 751 req)
- nv_gw 6h status: 200×714 / 502×36 / 400×1 → SR = 714/751 = **95.07%**
  (扣 400×1 BadRequest 后 714/750 = 95.20%)
- 6h 502=36 分类: zombie_empty_completion×16 / all_tiers_exhausted×10 / stream_first_byte_timeout×7
  / NVStream_IncompleteRead×2 / NVAnth_IncompleteRead×1 + BadRequest×1. **全已知类, 0 新可配置类**.
- vs R2045 6h SR95.40% (705/739) → 本轮 95.07% -0.33pp 区间内偏移非退化仍稳态区间.
  vs R2042 6h SR96.23% (765/795) → -1.16pp 区间内偏移 (R2042→R2045→本轮 96.23→95.40→95.07,
  缓慢下行但仍 >95% 稳态下沿, 非退化需持续观测是否逼近 94% 警戒).

### abs_cap
- 30min=0 / 6h=0 (DB `error_type ilike '%abs%' or ilike '%cap%'` 0 rows 双确认归零).
  R1918 方案0 cap_origin 重置持续归零, 连续多轮. 日志中 NV-PEEK-CAP-RESET / NV-CAP-RESET-MSFB
  是方案0 reset 事件非真 abs_cap 502.

### tier 上浮探测
- 6h nv_requests 502 中 `ilike '%429%' or ilike '%conn%' or ilike '%Integrate%' or ilike '%empty_200%'` = 0 rows
  → tier 层 pexec_429/pexec_conn/pexec_500/empty_200 全在 tier retry 内被吸收未上浮, 非新可配置类不需动.

### fallback (cc4101 30min) — 负向核心指标
- FALLBACK-OK = **9** (0 真中断, 0 fallback 失败): `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` = 0.
- 全 9 条: 7×75s `PRIMARY-FAIL-SKIP-CIRCUIT` (header/ttfb timeout 75s < chain budget 120s,
  cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 preempt 层)
  + 1×13.8s conn RemoteDisconnected (req=23211ada @13:57:58 = **重启 SIGTERM 打断的那条**, ms 救回 19.3s)
  + 1×unknown (req=0cb60d0b @13:41:41, ms 救回 9.6s)
- **120s 跑满类连续第 5 轮 0 条** (R2039=0 → R2040=0 → R2042=0 → R2045=0 → 本轮=0, 偏好持续回落).
- 重启窗口期 1 条 RemoteDisconnected 被 ms_gw 兜住 = **40007 热备机制实战验证** (CLAUDE.md 铁律
  "40007 是 restart nv_gw 时的热备" 真实生效, 0 真中断).
- 注: R2045 fallback 12 (11×75s SKIP-CIRCUIT + 1×unknown) → 本轮 9 (-3 区间内波动, 120s 跑满类仍 0 条).
- 全 9 条被 cc4101 抢断/打断切 ms, ms 救回 → CC 收 0 真 502.

### breaker
- cc4101 PRIMARY-BREAKER-OPEN 30min = **0**
- nv_gw 30min `grep -cE "BREAKER-FAIL|BREAKER.*OPEN|NV-ANTH-BREAKER-FAIL"` = **0**
- vs R2045 nv_gw recorded 30min=0 → 本轮仍 0 (state CLOSED count 仍 2, 未累积到 3)
  = **连续第 14 轮验证未恶化机制正常吸收非恶化** (符合 CLAUDE.md "recorded 但 CLOSED 是机制正常吸收").
  nv_breaker recorded failure 计数仍 2, 仍 CLOSED 未达 OPEN 阈值 (R1719 breaker OPEN 需连续累积).

### BUG-A 修复 (R1913) 在日志中真实生效确认
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **0 次** (重启后 12min, 流量 ramp up 样本太少属正常,
  非机制失效; 6h/历史持续触发确认机制长期生效, R2045 9 次).

### nv_gw /health + StartedAt 核实
- /health = ok (proxy_role=passthrough, nv_num_keys=5,
  nvcf_pexec_models=[kimi_nv,dsv4p_nv,glm5_2_nv], nv_default_model=dsv4p_nv, port=40006)
- nv_gw StartedAt = **2026-07-20T05:57:58Z** (UTC = CST 13:57:58; **连续 9 轮未漂移后真实漂移发生**,
  R2042 STATE 下一轮关切 "关注是否再漂移" 已应答: 漂移发生, 非 cc2 改, 非 OOM, SIGTERM 主动重启 + config-hash 变)
- cc4101 StartedAt = 2026-07-19T12:10:22Z (0 restart 未变, R1926 step2.0 env up-d 后)
- docker ps nv_gw "Up 12 minutes" 与 StartedAt 13:57:58 CST 到当前 14:10 CST (12min) 吻合
- RestartPolicy=unless-stopped, RestartCount=0 (非自动重启策略触发, 是外部主动 stop+start)
- 日志最早 `restored from /app/logs/glm52_mode_idx.json` (启动头) → 尾部 14:10:14,
  重启后稳定运行 12min 无崩溃, RR/integrate/glm5_2_nv pexec 全在跑.

## 决策: 继续 NOP (连续第 76 轮冻结)
- 数据全部满足"稳态":
  - 6h SR 95.07% > 95% 真实稳态 (vs R2045 -0.33pp 区间内偏移非退化, 仍稳态区间)
  - 30min SR 88.29% 含 13:57 重启窗口期属预期, 502 全 NVCF 已知三类不可配
  - abs_cap 30min/6h 双确认归零
  - fallback 9 条全兜住 0 真中断 0 失败, 120s 跑满类连续 5 轮 0 条偏好
  - breaker 30min recorded 0 条 (连续第 14 轮验证未恶化)
  - 重启窗口期 1 条 RemoteDisconnected 被 ms_gw 兜住 = 40007 热备实战验证
- 冻结理由 (连续第 76 轮) 仍成立: 半成品未 in-vivo 验证 (env 开关从未激活) + 激活需同步
  chain_budget 120→420 + cc4101 header 60→450 + post-200 软挂换 key 未实现 + 24h 观测窗口,
  风险/收益不对等 (当前稳态, 边际收益小).

## 状态变化
- **nv_gw StartedAt 漂移**: 03:02:10Z → 05:57:58Z (连续 9 轮未漂移后真实漂移, 非 cc2 改,
  非 OOM, SIGTERM 主动重启 + config-hash 变 0fd6cdbc→18fb44dd; peer oc2 R2044 已记录).
- **STATE env 快照纠正**: R2042 STATE 记 UPSTREAM_TIMEOUT=66 是过时值, 实际当前生产 90;
  NVU_TIER_BUDGET_GLM5_2_NV 实际 120 (oc2 R2043 记 115). 本轮 cc2 未改任何 env, 仅记录真实值.
- 6h SR 95.40%(R2045) → 95.07%(本轮) -0.33pp 区间内偏移, 仍稳态但缓慢下行, 需持续观测是否逼近 94% 警戒.
- 0 改动 0 restart (本轮 cc2 未动任何文件; nv_gw 重启是外部在本 session 之前做的, 非 cc2 改).

## 下一轮该做什么
- 继续拉数据确认趋势: 重点看 6h SR 是否仍在 94-96.9% 区间 (非退化), 502 分类是否仍是已知三类,
  fallback 是否仍全 75s SKIP/timeout 被兜住 (0 fallback 失败 0 真中断), BUG-A SKIP-PEXEC2 是否持续触发
  (重启后流量 ramp up, 下轮应能恢复触发计数).
- 关注 nv_gw StartedAt 是否再次漂移: 本轮漂移到 05:57:58Z (连续 9 轮未漂移后真实漂移),
  下一轮 docker inspect 核实是否仍 05:57:58Z; 若再次变化且非 cc2 改, 记录时间点供后续排查.
- 关注 6h SR 缓慢下行趋势 (96.23→95.40→95.07): 若持续逼近 94% 警戒才考虑动, 当前仍 >95% 不动.
- 轮号: 下一轮 git pull 看最新, peer oc2 可能已抢 R2046+; cc2 用 R2047 或更大 hm2_cc2 前缀不撞号.
- 若未来要解冻: 需先 in-vivo 验证 NVU_GLM52_EXP_BACKOFF (env 激活 + chain_budget 120→420 +
  cc4101 PRIMARY_HEADER_TIMEOUT 60→450 同步), 且实现 post-200 软挂换 key, 再 24h 观测. 当前不动.
