# R2036 (HM2 cc2): NOP 巡检 R134 — 连续第 70 轮冻结指数退避

**日期**: 2026-07-20
**轮号**: R2036 (hm2_cc2 前缀避 peer HM1 撞号; peer 最新 R2035 hm2_optimize_hm1, 尚未到 R2036)
**模式**: NOP 巡检, 0 改动 0 restart
**上一轮**: R2035 (313914c, cc2 R133) — 连续第 69 轮冻结

## 状态变化 (本 session 核实)

**nv_gw StartedAt 未再漂移**: 本轮 docker inspect 核实 nv_gw StartedAt = **2026-07-20T03:02:10Z**
(UTC, 对应本地 CST 11:02:10), 与 R2030/R2031/R2032/R2034/R2035 记录完全一致, 本轮期间未再重启, 连续第 5 轮核实未漂移。
env 0 变更 (RestartCount 字段 docker inspect 返回 None — 本机 docker 版本 State.RestartCount 字段缺失, 用 StartedAt 未变 + env 快照一致双重确认未 restart)。
env 快照与 R2035 完全一致 (KEY_COOLDOWN=25 / TIER_COOLDOWN=25 /
UPSTREAM_TIMEOUT=66 / TIER_TIMEOUT_BUDGET=180 / MIN_OUTBOUND=0 / NVU_GLM52_EXP_BACKOFF 不在 env=关)。
cc4101 StartedAt 仍 2026-07-19T12:10:22Z (R1926 后 0 restart, 未变)。
docker ps 显示 nv_gw "Up About an hour" 与 StartedAt 03:02:10Z (UTC, = 11:02:10 CST) 到当前 12:21 CST (约 79min) 完全吻合。
**R2035 STATE 下一轮关切 "关注 StartedAt 是否再漂移" → 本轮核实未再漂移 (连续第 5 轮)。**

## 改前数据 (本 session 拉取, 当前 CST 12:21, nv_gw StartedAt 11:02:10 CST)

### nv_gw 30min 窗口
- SR = 37/40 = **92.50%** (200:37 / 502:3)
- 502=3: all_tiers_exhausted×3
- **vs R2035 30min 95.74% (45/47) -3.24pp 区间内完全持平非退化** (200 45→37 -8 / 502 2→3 +1, 30min 流量 47→40 -7 req 区间内小波动 nv_gw 仍在干活)
- 30min 502=3 (vs R2035 502=2 +1 小样本波动): **本轮 30min 无 NVStream_IncompleteRead (R2035 有1), 3 条全 all_tiers_exhausted (NVCF 上游全 tier 耗尽, 已知类)** — 全已知类 0 新可配置类
- 注: 30min SR 92.50% 略低于巡检阈值 94%, 但 30min 流量仅 40 req 小样本, 502 全 all_tiers_exhausted (NVCF 上游 tier 全耗尽, 非 nv_gw 可配置类, 不可配), 6h 大样本 SR 96.72% (914 req) 是真实稳态 — 30min 低 SR 是小样本噪声非退化

### nv_gw 6h 窗口
- SR = 884/914 = **96.72%** (200:884 / 502:30)
- 502=30: zombie_empty_completion×13 / all_tiers_exhausted×9 / stream_first_byte_timeout×6 / NVAnth_IncompleteRead×1 / NVStream_IncompleteRead×1
- **vs R2035 6h 96.87% (929/959) -0.15pp 区间内完全持平非退化** (200 929→884 -45 / 502 30→30 持平, 双降 = 6h 滚动窗口前端旧数据滚出, 非退化)
- 6h 502=30 (vs R2035 502=30 完全一致): zombie 13→13 持平 / all_tiers_exhausted 8→9 +1 / stream_first_byte_timeout 7→6 -1 / NVAnth_IncompleteRead 1→1 持平 / NVStream_IncompleteRead 1→1 持平. **0 新可配置类, 0 重新分布大类**

### tier 30min error_type
- pexec_success×32 / pexec_conn_RemoteDisconnected×8 / pexec_429×2
- **vs R2035 tier 30min (pexec_success 31 / pexec_429 11 / pexec_conn 4 / pexec_500 3 / empty_200 1)**: 本轮 pexec_success 31→32 +1 / pexec_429 11→2 -9 / pexec_conn 4→8 +4 / pexec_500 3→0 -3 / empty_200 1→0 -1 — 全 tier 噪声同比流量小波动, 无新增类
- **关键**: 6h nv_requests 502 中 (status!=200) `ilike '%429%' or ilike '%conn%' or ilike '%Integrate%' or ilike '%empty_200%'` = **0 rows** → tier 层的 pexec_429/pexec_conn/empty_200/pexec_500 全在 tier retry 内被吸收, **未上浮为 nv_gw 502**
- 即: 上游 NVCF 429/500 抖动 + IntegrateRemoteDisconnected (NV integrate 子链路断连) + empty_200 (上游返回空 200) 是 tier 层的 retry 噪声, 被 tier retry 吃掉不上浮, **非新可配置类不需动** (符合 CLAUDE.md "tier 层错误被 retry 吸收 = 正常")

### abs_cap (双重确认归零)
- 30min `error_type ilike '%abs%' or ilike '%cap%'` = **0 rows**
- 6h `error_type ilike '%abs%' or ilike '%cap%'` = **0 rows**
- 连续多轮归零 (R1918 方案0 cap_origin 重置持续生效): R2035=0 → 本轮=0

### fallback (cc4101 30min, 6 条全 FALLBACK-OK, 0 真中断 0 失败)
- 6 条 FALLBACK-OK (vs R2035 8 条 -2 区间内波动):
  - 5 条 75s `PRIMARY-FAIL-SKIP-CIRCUIT` (header/ttfb timeout 75s < chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层, req d56fc85b(21544ms fb)/36f7edf5(44080ms fb)/4cd2b21c(6321ms fb)/f94c250a(14959ms fb)/389861f5(1995ms fb))
  - **1 条 120s chain budget 跑满类** (req=ddf65493 @12:19:45.7, 120105ms — header/ttfb timeout after 120s = chain budget 120s 跑满, 被 ms 救回 3036ms fb)
- **vs R2035 fallback 8 条 (全 8×75s SKIP-CIRCUIT, 0 条 120s 跑满)** → 本轮 120s 跑满类重现 1 条 (R2035=0 → 本轮=1, 偏升 1 条但非新类, 仍 75s/120s 已知类)
- 注: 120s 跑满类 R1951 4 → R1954 0 → ... → R2034 1 → R2035 0 → 本轮 1 (区间内波动, 单条), 仍全 fallback 在 75s/120s SKIP/timeout 层被兜住, 无跑满 fallback 失败
- **关键**: `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` cc4101 30min = **0** → 0 真中断, CC 收 0 真 502
- 注: 日志见 `NV-MS-FB-SERVED` — 这是 nv_gw 内部 ms_fb 路径记 breaker failure, state 仍 CLOSED, 非 NV-ANTH-BREAKER-FAIL 事件, 与 breaker OPEN 无关

### breaker (cc4101 + nv_gw, state CLOSED 非 OPEN 切流)
- cc4101 PRIMARY-BREAKER-OPEN 30min = **0**
- nv_gw 30min `grep -cE "BREAKER-FAIL|BREAKER.*OPEN|NV-ANTH-BREAKER-FAIL"` = **0** (vs R2035=0, 持平)
  - R2032/R2034 那条 NV-ANTH-BREAKER-FAIL recorded 事件 (req=42cc4e87 @11:34:58.8 zombie_empty_completion) 已滚出 30min 窗口 (当前 12:21 CST, 11:34 已 47min 前)
  - **本轮 30min 无新 NV-ANTH-BREAKER-FAIL 事件 = 连续偏好, nv_breaker 未新增 recorded failure**
- nv_breaker recorded failure 计数仍 2 (R2035 核实), **仍 CLOSED 未达 OPEN 阈值 (R1719 breaker OPEN 需连续累积, count=2 仍远未触发 OPEN 切流)**
- 即: R1719 mid-stream breaker 真实触发记录后, **state CLOSED count 未累积非切流 = 机制正常吸收, 不是恶化** (符合 CLAUDE.md "recorded 但 CLOSED 是机制正常吸收, 不是恶化")
- **连续第 8 轮 (R2027/R2029/R2030/R2031/R2032/R2034/R2035/R2036) 同事件验证未恶化**: R2027 首次触发 → R2029/R2030 30min 仍覆盖同事件 → R2031 滚出窗口无新事件 → R2032 新事件 (req=42cc4e87) 出现仍 CLOSED count 仍 2 → R2034 30min 仍覆盖 R2032 同事件 count 仍 2 state 仍 CLOSED → R2035/R2036 R2032 事件滚出窗口无新事件 count 仍 2 state 仍 CLOSED 非 OPEN 切流, 未恶化

### BUG-A 修复 (R1913) 在日志中真实生效确认
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **6 次** (R2005 3 / R2006 4 / R2007 5 / R2009 5 / R2011 4 / R2013 5 / R2014 7 / R2015 6 / R2016 6 / R2018 6 / R2019 8 / R2020 7 / R2021 8 / R2022 7 / R2023 7 / R2025 5 / R2026 6 / R2027 5 / R2029 5 / R2030 4 / R2031 5 / R2032 5 / R2034 6 / R2035 8 / 本轮 6, 区间内波动持续触发确认机制长期生效)

## 决策

**本轮 NOP (连续第 70 轮冻结)**, 0 改动 0 restart。依据:
- 6h SR 96.72% (>95% 稳态阈值, vs R2035 -0.15pp 区间内完全持平非退化)
- 30min SR 92.50% 略低 (但 30min 流量仅 40 req 小样本, 502 全 all_tiers_exhausted NVCF 上游已知类不可配, 6h 大样本才是真实稳态)
- 502 全 NVCF 上游已知三类 + NVAnth/NVStream_IncompleteRead (上游软挂同 zombie 性质), 0 新可配置类, 0 重新分布大类
- abs_cap 30min/6h 双确认归零
- fallback 6 条全兜住 (5×75s SKIP-CIRCUIT + 1×120s 跑满) 0 真中断 0 失败
- breaker cc4101=0, nv_gw recorded 0 (R2032 事件滚出窗口) state CLOSED count 仍 2 非 OPEN 切流, 连续第 8 轮验证未恶化
- BUG-A SKIP-PEXEC2 持续触发 6 次
- tier 上浮探测 6h = 0 rows, tier retry 吸收未上浮非新可配置类不需动

**冻结理由 (连续第 68 轮) 仍成立**: 半成品未经 in-vivo 验证 (env 开关从未激活, NVU_GLM52_EXP_BACKOFF 根本不在容器 env 中) + 激活需同步 chain_budget 120→420 + cc4101 header 60→450 + post-200 软挂换 key 未实现 + 24h 观测窗口. 风险/收益不对等 (当前 6h SR96.72% 0 真中断, abs_cap 连续多轮归零, BUG-A 修复真实生效, 120s 跑满类单条波动, 边际收益小).

## 验证结果

- nv_gw /health = ok (proxy_role=passthrough, nv_num_keys=5, models=[kimi_nv,dsv4p_nv,glm5_2_nv], default=dsv4p_nv, port=40006)
- docker inspect StartedAt 核实 nv_gw=03:02:10Z (R2035 后未再漂移, 连续第 5 轮核实) / cc4101=12:10:22Z (0 restart 未变)
- env 0 变更, 0 restart (NOP 轮)
- 状态变化: 无

## 下一轮该做什么

- **继续 NOP 巡检 (R135)**: 数据全部满足"稳态" (6h SR 96.72%>95%, 30min SR 92.50% 小样本偏低但 502 全 NVCF 已知类 + 6h 大样本真实稳态, abs_cap 归零, fallback 全兜住 0 真中断, breaker 30min recorded 0 条 R2032 事件已滚出窗口连续第 8 轮验证未恶化 count 仍 2 state 仍 CLOSED 非 OPEN 切流, BUG-A 持续触发). 冻结理由 (连续第 68 轮) 仍成立: 半成品未 in-vivo 验证 + 激活需同步 3 组件改动 + 24h 观测窗口, 风险/收益不对等.
- **拉数据确认趋势**: 重点看 6h SR 是否仍在 94-96.9% 区间 (非退化), 502 分类是否仍是已知三类 (zombie / stream_first_byte_timeout / all_tiers_exhausted) + NVAnth/NVStream_IncompleteRead (上游软挂同 zombie 性质, 0 新可配置类), fallback 是否仍全 75s/120s SKIP/timeout 被兜住 (0 fallback 失败 0 真中断), BUG-A SKIP-PEXEC2 是否持续触发. **关注 120s 跑满类 fallback 是否持续出现并增多** (本轮重现 1 条 req=ddf65493, R2035=0 → 本轮=1) — 若持续增多并逼近 fallback 失败才考虑动 chain_budget; 当前单条被 ms 救回 0 失败不需动. **nv_gw 30min BREAKER-FAIL recorded 已 0 (R2032 事件滚出窗口) — 下一轮继续看是否仍 0 (无新 breaker recorded 事件 = 偏好) 或出现新事件; 若出现新 NV-ANTH-BREAKER-FAIL 且 count 继续累积到 OPEN 阈值真切流才算恶化**; recorded 但 CLOSED 是机制正常吸收, 不是恶化. 任一指标恶化 (6h SR<94% 或 502 新可配置类或 fallback 失败或 breaker 真 OPEN 切流) 才考虑动.
- **关注 nv_gw StartedAt 是否再次漂移**: 本轮核实 nv_gw StartedAt 仍 03:02:10Z (R2030 首次发现漂移后未再变, 连续第 5 轮核实). 下一轮 docker inspect 核实 StartedAt 是否仍 03:02:10Z; 若再次变化且非 cc2 改, 记录时间点供后续排查 (可能是宿主重启或 OOM kill 自恢复).
- **轮号**: 下一轮 git pull 看最新, peer HM1 可能已抢 R2036+ hm2_optimize_hm1 前缀; cc2 用 R2037 或更大 hm2_cc2 前缀不撞号.
- **若未来要解冻**: 需先 in-vivo 验证 NVU_GLM52_EXP_BACKOFF (env 激活 + chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 同步), 且实现 post-200 软挂换 key, 再 24h 观测. 当前不动.
