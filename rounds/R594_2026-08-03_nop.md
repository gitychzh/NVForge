# R594 — NOP 巡检轮 (2026-08-03 11:30 CST)

## 数据 (轮前链路分析注入, 30min 窗口)
- cc2 (cc4101-primary): 0 req (session 间歇空闲, 无 cc2 评估样本)
- dsv4p_nv 30min: 11 req, 6×200 + 5×429 (SR=54.5%, hermes+openclaw caller)
  - per-key: key2 5×200 (100% 200, IP 203.10.96.139, avg_dur=10031ms)
  - per-key: key3 1×200 (100% 200, IP 134.195.101.194, avg_dur=3274ms)
  - 空 key 5×429 (avg_dur=2314ms 快速返回, NVCF 配额型拒绝)
- 唯一错误 `all_tiers_exhausted` ×5 (NVCF 配额型, 非 nv_gw tier 故障)
- finish_reason: tool_calls×4 + stop×2 (健康, 无 zombie stop)
- fallback 发生率 f×11 (cc4101 层 ms_gw 兜底, dsv4p_nv 跳 peer/ms fb → nv_gw 裸返, 预期)
- 无 buffer/wait 日志 (30min 无 buffer 触发)
- 无 stream_total_deadline, 无 zombie, 无新错误类型

## 判稳
- SR=54.5% vs R593 69.2% vs R592 66.7% vs R591 44.4% vs R590 37.5% → 波动区间内 (37.5%-71.4%)
- 与 R545-R593 同一 NVCF 配额波动模式: 命中可用 key 时 100% 200, 全挂时空 key 429
- cc2 0 流量 → 铁律1 cc2 视角不满足 → 不动码
- 无新错误, 无参数漂移, 健康检查 ok → 无介入必要

## 本轮改动
- 无 (NOP)

## 验证
- 0 restart → 无需 py_compile / curl 复测
- curl /health: status=ok, nv_num_keys=5, models=[kimi_nv,dsv4p_nv,glm5_2_nv]
- docker ps: nv_gw Up 21h, nv_gw_stable Up 34h, cc4101 Up 11h, ms_gw Up 4 days, logs_db Up 4 days

## 下一步
- 继续 NOP 巡检, 等 cc2 流量恢复后观察 dsv4p_nv buffer 路径行为
- 关注新错误类型或 key/IP 级故障再决定介入
- dsv4p_nv 小时级 SR <60% + cc2 流量恢复 → 评估 TIER_COOLDOWN_S 180s 是否过激
- all_tiers_exhausted 中段不恢复再评估 (当前 ~5/30min 全 NVCF 配额型)
