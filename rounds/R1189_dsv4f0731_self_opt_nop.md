# R1189: dsv4f0731_nv40666 NOP — 30min 全绿 (SR=100%, 0错误, 0 fallback, 0 429)

日期: 2026-08-08 08:44 UTC 采集窗口 (~30min)
容器: `dsvf0731_nv40666` (端口 40666, num_keys=5, DSV4F0731 NVCF 链路)

## 决策：NOP（不改任何参数）

连续第二十轮全绿（R1170~R1189 均 SR=100%），健康稳态，守"改前必有数据"铁律不动作。

## 30min 窗口数据（脚本注入）

| 指标 | 值 |
|---|---|
| SR | **100%** (168/168, 0 error) |
| 错误 / Fallback | **0 / 0** |
| Avg / P50 / P95 | 11092 / 8307 / 29277 ms |
| 429 | 0 |
| upstream_type | nvcf_pexec 168/168 (100%) — 全 pexec，无 integrate |
| finish_reason | tool_calls 142, stop 26 |

**Per-key 200 延迟**（全 5 key 无错误，健康）:
```
key0|34req|avg11607|P9528099
key1|34req|avg11136|P9529882
key2|32req|avg8232|P9516465
key3|35req|avg12586|P9536511
key4|33req|avg11705|P9529393
```
5 key 负载均匀 (32-35 req/key)、延迟同量级 (8.2-12.6s avg)，k2 略快但无错误聚集，无劣化 key。per-key 错误为空——30min 内 **0 错误**。

**tier_attempts**: 空（30min 内 0 错误，无 key 切换失败）。

**key_cycle_429s**: 0|74, 1|94 — 与 R1188 (0|63, 1|83) 略升为噪声波动，30min 429=0 验明无 429/循环压力。

## 趋势

- **6h**: 2012/2007 → **SR=99.75%**，5 失败，0 fallback（5 失败均落在 30min 窗口之外，为长期稀疏速率，非本窗口信号）
- **3h 逐小时**: 00:00 241(100%), 23:00 316(100%), 22:00 265(100%*), 21:00 98(100%) — 高流量且逐小时 SR 全 ≥98.9%（22:00 3 error 为更早窗口已确认的稀疏事件）
- **24h all_tiers_exhausted**: 44（~1.8/h，均被 fallback 兜住，非本 30min 窗口信号，与 R1188/R1187 持平）

## 关键判断

- 30min SR=100%、0 错误、0 fallback、0 429 — **连续第二十轮全绿**（R1170~R1189 均 100%，窗口请求量 98-316、延迟 avg 稳定在 8.2-13.7s 带）。
- 全部 pexec、无 key 劣化、per-key 分布均匀 (32-35/key)、avg 8.2-12.6s 稳定。key_cycle_429s 略有噪声波动但 429=0，无压力。
- 6h SR=99.75% + 逐小时高流量（最高 316/h）稳定 SR ≥98.9% → 整体链路完全健康稳态，无任何退化信号。
- P95 带 16.5-36.5s 为 NVCF pexec 正常稀疏尾部，SR 仍 100%（P95 波动属噪声，R1188 为 16.7-39.1s，量级一致）。

**无任何调参依据。** 守"改前必有数据"铁律——健康窗口不动作，避免为调而调。

## 上次修改效果 (R1188 → R1189)

R1188 SR=100% (146/146)。本轮 SR=**100%** (168/168)，0 错误，连续全绿再次验证 NOP 判断正确。per-key 延迟带 9.0-12.3s → 8.2-12.6s（同量级稳定，噪声内）。key_cycle_429s 噪声波动（k0 63→74, k1 83→94）但 429=0 无实际压力。**无需要修的参数。**

## 下一步建议

- 保持观察。连续二十轮全绿强 NOP 信号，下轮若仍无 429 回升 / 错误聚集，继续 NOP。
- 预置对策（仅当信号出现才动作，沿用之前设定）:
  - **429 回升 ≥5/30min** → `KEY_COOLDOWN_S` 30→60s。
  - **all_tiers_exhausted ≥3/30min 且单 key 聚集** → 检查该 key 出口 IP / 代理质量，非直接改参。
  - **NVCF 错误聚集**（同种 ≥3/30min） → 排查上游出口而非盲目调超时（单次事件已被 key 循环兜住，不足为凭）。
- 容器 6h /health OK、全 5 key 正常、no integrate 激活—保持现有配置不变。