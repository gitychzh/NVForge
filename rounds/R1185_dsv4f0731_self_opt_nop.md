# R1185: dsv4f0731_nv40666 NOP — 30min 全绿 (SR=100%, 0错误, 0 fallback, 0 429)

日期: 2026-08-08 08:24 UTC 采集窗口 (~30min)
容器: `dsvf0731_nv40666` (端口 40666, num_keys=5, DSV4F0731 NVCF 链路)

## 决策：NOP（不改任何参数）

连续第十六轮全绿（R1170~R1185 均 SR=100%），健康稳态，守"改前必有数据"铁律不动作。

## 30min 窗口数据（脚本注入）

| 指标 | 值 |
|---|---|
| SR | **100%** (152/152, 0 error) |
| 错误 / Fallback | **0 / 0** |
| Avg / P50 / P95 | 11553 / 9286 / 39819 ms |
| 429 | 0 |
| upstream_type | nvcf_pexec 152/152 (100%) — 全 pexec，无 integrate |
| finish_reason | tool_calls 127, stop 25 |

**Per-key 200 延迟**（全 5 key 无错误，健康）:
```
key0|30req|avg11990
key1|31req|avg10454
key2|28req|avg11872
key3|31req|avg9964
key4|32req|avg13468
```
5 key 负载均匀 (28-32 req/key)、延迟同量级 (10.0-13.5s avg)，无劣化 key。per-key 错误为空——30min 内 **0 错误**。

**tier_attempts**: 空（30min 内 0 错误，无 key 切换失败）。

**key_cycle_429s**: 0|68, 1|84 — 与 R1184 (0|65, 1|87) 基本持平，纯噪声无增长。30min 429=0 验明无 429/循环压力。

## 趋势

- **6h**: 1999/2005 → **SR=99.75%**，5 失败，0 fallback（5 失败均落在 30min 窗口之外，为长期稀疏速率，非本窗口信号）
- **3h 逐小时**: 00:00 122(100%), 23:00 316(100%), 22:00 262/265(98.9%), 21:00 209(100%) — 高流量且逐小时 SR 全 ≥98.9%
- **24h all_tiers_exhausted**: 47（~2.0/h，均被 fallback 兜住，非本 30min 窗口信号，与 R1184 持平）

## 关键判断

- 30min SR=100%、0 错误、0 fallback、0 429 — **连续第十六轮全绿**（R1170~R1185 均 100%，窗口请求量 56-316、延迟 avg 稳定在 10.0-13.5s 带）。
- 全部 pexec、无 key 劣化、per-key 分布均匀 (28-32/key)、avg 10.0-13.5s 稳定。key_cycle_429s 与上轮基本持平（k0 65→68, k1 87→84），零增长。
- 6h SR=99.75% + 逐小时高流量（最高 316/h）稳定 SR ≥98.9% → 整体链路完全健康稳态，无任何退化信号。
- P95 带 39.8s 为 NVCF pexec 正常稀疏尾部，SR 仍 100%（P95 波动属噪声，R1184 为 28.9s，量级皆在正常尾部）。

**无任何调参依据。** 守"改前必有数据"铁律——健康窗口不动作，避免为调而调。

## 上次修改效果 (R1184 → R1185)

R1184 SR=100% (154/154)。本轮 SR=**100%** (152/152)，0 错误，连续全绿再次验证 NOP 判断正确。per-key 延迟带 9.2-13.7s → 10.0-13.5s（同量级稳定，噪声内）。key_cycle_429s 基本持平（k0 65→68, k1 87→84），零增长无 429 压力。**无需要修的参数。** R1183 预置的应对策略均未触发（429=0、无错误聚集、无 IncompleteRead 聚集）。

## 下一步建议

- 保持观察。连续十六轮全绿强 NOP 信号，下轮若仍无 429 回升 / 错误聚集，继续 NOP。
- 预置对策（仅当信号出现才动作，沿用之前设定）:
  - **429 回升 ≥5/30min** → `KEY_COOLDOWN_S` 30→60s。
  - **all_tiers_exhausted ≥3/30min 且单 key 聚集** → 检查该 key 出口 IP / 代理质量，非直接改参。
  - **NVCF 错误聚集**（同种 ≥3/30min） → 排查上游出口而非盲目调超时（单次事件已被 key 循环兜住，不足为凭）。