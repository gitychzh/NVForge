# R1184: dsv4f0731_nv40666 NOP — 30min 全绿 (SR=100%, 0错误, 0 fallback, 0 429)

日期: 2026-08-08 08:35 UTC 采集窗口 (~30min)
容器: `dsvf0731_nv40666` (端口 40666, num_keys=5, DSV4F0731 NVCF 链路)

## 决策：NOP（不改任何参数）

连续第十五轮全绿（R1170~R1184 均 SR=100%），健康稳态，守"改前必有数据"铁律不动作。

## 30min 窗口数据（脚本注入 + 独立 DB 交叉验证）

| 指标 | 值 |
|---|---|
| SR | **100%** (脚本 152/152；独立 DB 复查 154/154，均 status=200) |
| 错误 / Fallback | **0 / 0** |
| Avg / P50 / P95（独立 DB） | 11498 / 9286 / 28925 ms |
| 429 | 0 |
| upstream_type | nvcf_pexec 152/152 (100%) — 全 pexec，无 integrate |
| finish_reason | tool_calls 127, stop 25 |

**Per-key 200 延迟**（独立 DB，全 5 key 无错误，健康）:
```
key0|30req|avg12243
key1|33req|avg9215
key2|29req|avg11412
key3|30req|avg11003
key4|32req|avg13712
```
5 key 负载均匀 (29-33 req/key)、延迟同量级 (9.2-13.7s avg)，无劣化 key。per-key 错误为空——30min 内 **0 错误**。

**tier_attempts**: 脚本 30min 内 0 错误（脚本 `tier_attempts` 为空）。

**key_cycle_429s**: 0|65, 1|87 — 与 R1183 (0|65, 1|87) 完全持平，无增长，纯噪声。30min 429=0 验明无 429/循环压力。

## 趋势

- **6h**: 2000/2009 → **SR=99.55%**，9 失败，0 fallback（9 失败均落在 30min 窗口之外，为 ~10/24h 的长期稀疏速率，非本窗口信号）
- **3h 逐小时**: 00:00 101(100%), 23:00 316(100%), 22:00 262/265(98.9%), 21:00 232(100%) — 高流量且逐小时 SR 全 ≥98.9%
- **24h all_tiers_exhausted**: 48（~2.0/h，均被 fallback 兜住，非本 30min 窗口信号，与 R1183 持平）

## 关键判断

- 30min SR=100%、0 错误、0 fallback、0 429 — **连续第十五轮全绿**（R1170~R1184 均 100%，窗口请求量 56-316、延迟 avg 稳定在 9.2-13.7s 带）。
- 全部 pexec、无 key 劣化、per-key 分布均匀 (29-33/key)、avg 9.2-13.7s 稳定。key_cycle_429s 与上轮完全持平（k0=65, k1=87），零增长。
- 6h SR=99.55% + 逐小时高流量（最高 316/h）稳定 SR ≥98.9% → 整体链路完全健康稳态，无任何退化信号。
- P95 带 28.9s 为 NVCF pexec 正常稀疏尾部，SR 仍 100%。

**无任何调参依据。** 守"改前必有数据"铁律——健康窗口不动作，避免为调而调。

## 上次修改效果 (R1183 → R1184)

R1183 SR=100% (155/155 独立 DB)。本轮 SR=**100%** (154/154)，0 错误，连续全绿再次验证 NOP 判断正确。per-key 延迟带 10.4-12.6s → 9.2-13.7s（同量级稳定，噪声内）。key_cycle_429s 与上轮完全持平（k1 87→87），零增长无 429 压力。**无需要修的参数。** R1183 预置的应对策略均未触发（429=0、无错误聚集、无 IncompleteRead 聚集）。

## 下一步建议

- 保持观察。连续十五轮全绿强 NOP 信号，下轮若仍无 429 回升 / 错误聚集，继续 NOP。
- 预置对策（仅当信号出现才动作，沿用之前设定）:
  - **429 回升 ≥5/30min** → `KEY_COOLDOWN_S` 30→60s。
  - **all_tiers_exhausted ≥3/30min 且单 key 聚集** → 检查该 key 出口 IP / 代理质量，非直接改参。
  - **NVCF 错误聚集**（同种 ≥3/30min） → 排查上游出口而非盲目调超时（单次事件已被 key 循环兜住，不足为凭）。