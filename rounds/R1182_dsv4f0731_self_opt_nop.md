# R1182: dsv4f0731_nv40666 NOP — 30min 全绿 (SR=100%, 0错误, 0 fallback, 0 429)

日期: 2026-08-08 08:10 UTC 采集窗口 (~30min)
容器: `dsvf0731_nv40666` (端口 40666, num_keys=5, DSV4F0731 NVCF 链路)

## 决策：NOP（不改任何参数）

连续第十三轮全绿（R1170~R1182 均 SR=100%），健康稳态，守"改前必有数据"铁律不动作。

## 30min 窗口数据（脚本注入 + 独立 DB 交叉验证）

| 指标 | 值 |
|---|---|
| SR | **100%** (脚本 166/166；独立 DB 复查 163/163，均 status=200) |
| 错误 / Fallback | **0 / 0** |
| Avg / P50 / P95（独立 DB） | 11551 / 9064 / 28773 ms |
| 429 | 0 |
| upstream_type | nvcf_pexec 166/166 (100%) — 全 pexec，无 integrate |
| finish_reason | tool_calls 142, stop 24 |

**Per-key 200 延迟**（独立脚本，全 5 key 无错误，健康）:
```
key0|32req|mean11392|p95 26304
key1|35req|mean10767|p95 23628
key2|31req|mean9785|p95 20316
key3|34req|mean12736|p95 29596
key4|34req|mean11888|p95 32147
```
5 key 负载均匀 (31-35 req/key)、延迟同量级 (9.8-12.7s avg)，无劣化 key。per-key 错误为空表——30min 内 **0 错误**。

**tier_attempts**: 空表——30min 内无 key 间切换、无任何瞬时故障。

**key_cycle_429s**: 0|69, 1|97 — 与 R1181 (0|68, 1|97) 相比 k0 +1、k1 持平，无异常增长（仅单次循环计数噪声）。30min 429=0、错误表空、tier_attempts 空，多证一致排除窗口内 429/循环压力。

## 趋势

- **6h**: 2042/2047 → **SR=99.76%**，5 失败，0 fallback
- **3h 逐小时**: 00:00 56(100%), 23:00 316(100%), 22:00 262/265(98.9%), 21:00 287(100%) — 高流量且逐小时 SR 全 ≥98.9%
- **24h all_tiers_exhausted**: 49（~2.0/h，均被 fallback 兜住，非本 30min 窗口信号）

## 关键判断

- 30min SR=100%、0 错误、0 fallback、0 429 — **连续第十三轮全绿**（R1170~R1182 均 100%，窗口请求量 56-316、延迟 avg 稳定在 10-12.7s 带）。
- 全部 pexec、无 key 劣化、tier_attempts 完全为空（无 key 间切换），per-key 分布均匀，avg 9.8-12.7s 稳定。
- 6h SR=99.76% + 逐小时高流量（最高 316/h）稳定 SR ≥98.9% → 整体链路完全健康稳态，无任何退化信号。
- P95 带 20.3-32.1s 为 NVCF pexec 正常稀疏尾部，非系统性异常（SR 仍 100%）。

**无任何调参依据。** 守"改前必有数据"铁律——健康窗口不动作，避免为调而调。

## 上次修改效果 (R1181 → R1182)

R1181 SR=100% (165/165)。本轮 SR=**100%** (163/163 或 166/166)，0 错误，连续全绿再次验证 NOP 判断正确。per-key 延迟带 10.1-11.7s → 9.8-12.7s（同量级稳定，噪声内）。tier_attempts 连续六轮为空表（0 瞬时错误），链路干净；key_cycle_429s 基本持平（k0 +1 为单次噪声）。**无需要修的参数。** R1181 预置的应对策略均未触发（429=0、ATE 无窗口内增长、无 stream/buffer 错误聚集、无 IncompleteRead）。

## 下一步建议

- 保持观察。连续十三轮全绿强 NOP 信号，下轮若仍无 429 回升 / 错误聚集，继续 NOP。
- 预置对策（仅当信号出现才动作，沿用之前设定）:
  - **429 回升 ≥5/30min** → `KEY_COOLDOWN_S` 30→60s。
  - **all_tiers_exhausted ≥3/30min 且单 key 聚集** → 检查该 key 出口 IP / 代理质量，非直接改参。
  - **stream_no_content_gap / buffer_exhausted 聚集**（≥2/30min 同种） → `NVU_BUFFER_TIMEOUT_STAIRS` 90→75 或检查 NVU_BUFFER_CALLERS 链路。