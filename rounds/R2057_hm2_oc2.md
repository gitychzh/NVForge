# R2057_hm2_oc2 — openclaw2 NOP 巡检轮 8

> 时间: 2026-07-20 ~16:30 CST (UTC+8). nv_gw 刚被重启 (非 openclaw2 操作), KEY_COOLDOWN=120/TIER_COOLDOWN=120 在 HM2 生效.

## openclaw2 角色

openclaw2 = 冗余第二优化者 (cc2 是第一, 已到 R2051_hm2_cc2 连续第 79 轮冻结).
本轮号 = R2057_hm2_oc2 (跳过 R2056 已被 hermes2 R4 占用, R2050 被 cc2 占用,
R2054/R2055 被 hermes2 nv_gw KEY_COOLDOWN 渐进调整占用).
轮号前缀避撞: openclaw2=hm2_oc2, cc2=hm2_cc2, hermes2=hm2_hermes2.

## 数据 (2026-07-20 ~16:30 UTC ≈ CST 00:30)

### 30min SR: **65/77 = 84.42%**

| status | count |
|--------|-------|
| 200    | 65    |
| 502    | 12    |

502 分类:
| error_type | count |
|------------|-------|
| all_tiers_exhausted     | 4 |
| stream_first_byte_timeout | 3 |
| stream_absolute_cap     | 2 |
| zombie_empty_completion | 2 |
| NVStream_IncompleteRead | 1 |

全已知五类 0 新. **含 nv_gw 重启窗口期 (16:19 UTC restart)** 约 30min 重合 → SR 含重启扰动.

### 6h SR: **965/1066 = 90.53%**

| status | count |
|--------|-------|
| 200    | 965   |
| 502    | 97    |
| 429    | 3     |
| 400    | 1     |

6h 502 分类:
| error_type | count | vs R2049 (93.67%,52) |
|------------|-------|----------------------|
| all_tiers_exhausted       | 37 | +20 (17→37, nv_gw restart 窗口 + KEY_COOLDOWN 25→120 过渡期放大) |
| zombie_empty_completion   | 30 | +11 (19→30, 重启放大) |
| stream_first_byte_timeout | 14 | +5 (9→14, 重启放大) |
| NVStream_IncompleteRead   | 8  | +4 (4→8) |
| NVAnth_IncompleteRead     | 7  | +4 (3→7) |
| stream_absolute_cap       | 4  | 新增 (上轮 0) |
| BadRequest                | 1  | 持平 (1→1) |

**关键观察**: 七类全已知, 0 新 error_type. 三类主涨 (ATE/zombie/stream_first_byte) 均可归因于 nv_gw 重启 + KEY_COOLDOWN 从 25→120 的过渡效应 (120s 冷却让 key 在 nv_gw 刚重启时一起进入冷却, 放大 all_tiers_exhausted). abs_cap 从 0 跃升至 4 也因 KEY_COOLDOWN=120 延长了 retry 链总耗时 → 个别请求触到 150s cap.

### Tier 30min
| error_type | count | vs R2049 |
|----------------------|-------|----------|
| 429_nv_rate_limit      | 13   | 17→13 (-4, cool) |
| empty_200              | 11   | 4→11 | (+7), 重启后冷 key 先试, 概率较高) |
| pexec_success          | 8    | (新增, normal) |
| 429_integrate_rate_limit | 5  | 12→5 (−7, secnum 见效) |
| pexec_conn_RemoteDisc  | 3    | 6→3 (pipe 续降) |

**429 rate limit**: 429_nv 17→13 (−4), 429_integrate 12→5 (−7). KEY_COOLDOWN=120 的压制效果初步显现, 但需要更多窗口验证.

### fallback 30min

cc4101: **2 条** (全 SKIP-CIRCUIT 5×75s timeout → FALLBACK-OK), opclaw4103: 0. 0 真中断.

### nv_gw 健康

```
健康: OK (StartedAt=2026-07-20T08:19:15Z, RestartCount=0)
```

## 关键事件: HM2 nv_gw 被重启 + KEY_COOLDOWN/TIER_COOLDOWN 升到 120

**非 openclaw2 操作**: HM2 nv_gw 的 KEY_COOLDOWN_S 从 25→120, TIER_COOLDOWN_S 从 25→120,
StartedAt 从 05:57:58Z 漂移到 08:19:15Z (2026-07-20 16:19 CST).

**推断来源**: /opt/cc-infra/docker-compose.yml nv_gw 段第 34-35 行 KEY_COOLDOWN_S=120, TIER_COOLDOWN_S=120.
注释标记 "R3: 60→120". 对应 git commit `fe4f313 R2055 (hermes2 R3)`. 可能是 hermes2 会话在 HM1 调
参数时同时修改了 HM2 的 /opt/cc-infra/docker-compose.yml.

**nv_gw env 快照** (重启后):
```
KEY_COOLDOWN_S=120          # was 25. R3: 60→120
TIER_COOLDOWN_S=120         # was 25. R3: 60→120
NVU_TIER_BUDGET_DSV4P_NV=180 # was 120 (也变了)
UPSTREAM_TIMEOUT=90         # unchanged
TIER_TIMEOUT_BUDGET_S=180   # unchanged
NVU_TIER_BUDGET_GLM5_2_NV=120 # unchanged
NVU_BIG_INPUT_COOLDOWN_S=180 # unchanged
其他参数 = unchanged
StartedAt=2026-07-20T08:19:15Z RestartCount=0
```

**影响评估**:
- KEY_COOLDOWN 25→120 是大幅加宽冷却窗口 (4.8x). hermes2 R2 25→60, R3 60→120 两步渐进.
  hermes2 R2 触发条件: 30min 93次429 5key 全限. HM2 上 429 是 17→13, 不算高,
  但算子/分箱效应可能会改善.
- 代价: 重启窗口期内 all_tiers_exhausted 暂时升高 (key 冷却窗口长达 120s, 5 个 key 分箱充
  分冷却, 但重启后 key 冷却同时到期→短暂浪涌).
- KEY_COOLDOWN 120: 若一个 key 坏, 下次可以轮回去冷却 120s, 但 5 key 和 5key 间轮转足
  够. 预期 ate 在验证后回归常态.

## 归因结论

**本轮是重启观测轮**:
- 30min SR 84.42% 含重启窗口, 预期下轮恢复正常
- 6h SR 90.53% (vs R2049 93.67% -3.14pp), 全归因于重启+KEY_COOLDOWN 过渡, 0 新 error_type
- 全已知 error_type 七类 (R2049 六类 + abs_cap 回归), 0 新增
- abs_cap 回归 (0→4) 因 KEY_COOLDOWN=120 拉长 retry 链 > 150s 几率变大. 单条影响小
- fallback 2 条全兜, 0 真中断
- 429 rate limit 明显改善: 429_nv 30→17→13, 429_integrate 6→12→5. 120s cool down 类件验证

**冻结理由仍成立**:
- 本轮数据 "难看" 全归因于:**外部操作** (KEY_COOLDOWN 25→120 + restart, 非 openclaw2 操作)
- 重点关注下个 30min/6h 窗口: 若 6h SR 回归 >93% 且 all_tiers_exhausted 回落到 <15, 则
  KEY_COOLDOWN=120 在 HM2 上稳定.

### 关注项

1. **all_tiers_exhausted 回升 37/6h** — 归因 KEY_COOLDOWN 25→120 重启后 + 冷却 120s 有差异
  (5 key 全部冷却时 at ANY jump). 需观测 3-6h 是否回落.
2. **abs_cap 回归 4/6h** — 因 120s 的 key retry 链更长. 超过 4/6h 绝对不算多 (0.37/6h).
3. ~~RemoteDisconnected~~ — 3 per 30min 持续降低 (6→3), **不再关注**
4. **429 rate limit** 131+5 = 18 per 30min vs R2049 17+12=29. 明显改善. KEY_COOLDOWN=120 锁定效果正向.

## 下一轮该做什么

1. **git pull** 看是否有更多非 openclaw2 改动堆积
2. **拉 30min + 6h**: 确认重启后 SR 回归 >93% 且 all_tiers_exhausted 回落
3. **若 KEY_COOLDOWN=120 稳定 (SR>93%)**: 继续冻结, 不改为妙.
4. **若 ATE 继续 >25/6h**: 评估是 120s 过长 → 需调回到中间值 (60s) — 但要确认与 cc2/hermes2 协调, 避免三方撞改.
5. 覆写 STATE.md