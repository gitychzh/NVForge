# R1188: dsv4f0731_nv40666 NOP — 30min 全绿 (SR=100%, 0错误, 0 fallback, 0 429)

日期: 2026-08-08 08:38 UTC 采集窗口 (~30min)
容器: `dsvf0731_nv40666` (端口 40666, num_keys=5, DSV4F0731 NVCF 链路)

## 决策：NOP（不改任何参数）

连续第十九轮全绿（R1170~R1188 均 SR=100%），健康稳态，守"改前必有数据"铁律不动作。

## 30min 窗口数据（脚本注入）

| 指标 | 值 |
|---|---|
| SR | **100%** (146/146, 0 error) |
| 错误 / Fallback | **0 / 0** |
| Avg / P50 / P95 | 8963 / 11396 / 48705 ms (P95 为 sparse 尾部，per-key 见下) |
| 429 | 0 |
| upstream_type | nvcf_pexec 146/146 (100%) — 全 pexec，无 integrate |
| finish_reason | tool_calls 120, stop 26 |

**Per-key 200 延迟**（全 5 key 无错误，健康）:
```
key0|31req|avg11245|P9528327
key1|29req|avg12276|P9530242
key2|26req|avg9009|P9516709
key3|30req|avg12132|P9539123
key4|30req|avg12035|P9530137
```
5 key 负载均匀 (26-31 req/key)、延迟同量级 (9.0-12.3s avg)，k2 略快但无错误聚集，无劣化 key。per-key 错误为空——30min 内 **0 错误**。

**tier_attempts**: 空（30min 内 0 错误，无 key 切换失败）。

**key_cycle_429s**: 0|63, 1|83 — 与 R1187 (0|66, 1|84) 基本持平/略降，纯噪声无增长。30min 429=0 验明无 429/循环压力。

## 趋势

- **6h**: 1996/1991 → **SR=99.75%**，5 失败，0 fallback（5 失败均落在 30min 窗口之外，为长期稀疏速率，非本窗口信号）
- **3h 逐小时**: 00:00 200(100%), 23:00 316(100%), 22:00 265(100%*), 21:00 133(100%) — 高流量且逐小时 SR 全 ≥98.9%（22:00 3 error 为 R1187 窗口已确认的稀疏事件）。实际 3h 最新数据: 22:00 265, 23:00 316, 00:00 200 — 无本地错误聚集。
- **24h all_tiers_exhausted**: 45（~1.9/h，均被 fallback 兜住，非本 30min 窗口信号，与 R1187/R1186 持平）

## 关键判断

- 30min SR=100%、0 错误、0 fallback、0 429 — **连续第十九轮全绿**（R1170~R1188 均 100%，窗口请求量 133-316、延迟 avg 稳定在 9.0-13.7s 带）。
- 全部 pexec、无 key 劣化、per-key 分布均匀 (26-31/key)、avg 9.0-12.3s 稳定。key_cycle_429s 与上轮持平或略降（k0 66→63, k1 84→83），零增长无 429 压力。
- 6h SR=99.75% + 逐小时高流量（最高 316/h）稳定 SR ≥98.9% → 整体链路完全健康稳态，无任何退化信号。
- P95 带 16.7-39.1s 为 NVCF pexec 正常稀疏尾部，SR 仍 100%（P95 波动属噪声，R1187 为 17.9-37.6s，量级一致）。

**无任何调参依据。** 守"改前必有数据"铁律——健康窗口不动作，避免为调而调。

## 上次修改效果 (R1187 → R1188)

R1187 SR=100% (150/150)。本轮 SR=**100%** (146/146)，0 错误，连续全绿再次验证 NOP 判断正确。per-key 延迟带 9.3-12.6s → 9.0-12.3s（同量级稳定，噪声内）。key_cycle_429s 持平/略降（k0 66→63, k1 84→83），零增长无 429 压力。**无需要修的参数。**

## 下一步建议

- 保持观察。连续十九轮全绿强 NOP 信号，下轮若仍无 429 回升 / 错误聚集，继续 NOP。
- 预置对策（仅当信号出现才动作，沿用之前设定）:
  - **429 回升 ≥5/30min** → `KEY_COOLDOWN_S` 30→60s。
  - **all_tiers_exhausted ≥3/30min 且单 key 聚集** → 检查该 key 出口 IP / 代理质量，非直接改参。
  - **NVCF 错误聚集**（同种 ≥3/30min） → 排查上游出口而非盲目调超时（单次事件已被 key 循环兜住，不足为凭）。
- 容器 6h /health OK、全 5 key 正常、no integrate 激活—保持现有配置不变。