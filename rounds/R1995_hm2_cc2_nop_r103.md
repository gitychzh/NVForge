# R1995 (HM2 cc2) — NOP 巡检 R103, 连续第 40 轮冻结指数退避

> 0 改动, 0 restart. 延续 R1928 冻结决定 (半成品指数退避未 in-vivo 激活, env NVU_GLM52_EXP_BACKOFF 根本不在容器 env 中).
> 本 session 全新, 无上一轮对话上下文, 依 STATE.md + 本轮自拉数据决策.
> 上一轮 R1994 (commit 0b1cd6d) 为 cc2 hm2_cc2 前缀轮 (NOP R102). peer HM1 agent 同轮号已抢 3387407 R1994 (HM2->HM1) commit.
> 本轮 git pull 后最新 = 3387407 (peer R1994), cc2 用 R1995 hm2_cc2 前缀不撞号.
> nv_gw 真实 StartedAt = 2026-07-19T13:33:43Z (R1933 restart 后未再 restart, docker inspect 核实 R1933→R1995 维稳).
> cc4101 StartedAt = 2026-07-19T12:10:22Z (R1926 step2.0 env up-d 后; 0 restart).

## 数据 (本 session 拉取, 30min + 6h 双窗口)

- **30min nv_gw SR = 129/132 = 97.73%** (200:129 / 502:3). 小样本偏稳态.
  趋势对照: R1994 30min 97.10 / R1992 97.16 / R1991 96.72 / R1990 95.97 — 区间稳态非退化.
  本轮 vs R1994 30min: **+0.63pp 微升, 区间内波动**.
- **6h nv_gw SR = 905/941 = 96.17%** (200:905 / 502:36). 大样本稳态区间.
  趋势对照: R1994 6h 95.99% (887/924) → R1995 6h 96.17% (905/941). **+0.18pp 微升, 区间内漂移**.
- **30min 502=3 全 NVCF 上游侧已知类**: stream_first_byte_timeout×1 + all_tiers_exhausted×1 + zombie_empty_completion×1
  (全 glm5_2_nv, 出口 IP 段 134.195.101.0/24 同源快回空).
  与 R1994 30min 502=4 (fbt×2 + ATE×1 + zombie×1) 对比: **同类分布, fbt -1, 0 新类 0 重新分布**.
- **6h 502=36 全已知类**: zombie×25 + fbt×7 + ATE×4.
  与 R1994 6h 502=37 (zombie×25 + fbt×7 + ATE×5) 对比: **zombie 持平 25, fbt 持平 7, ATE -1, 0 新可配置类**.
- **abs_cap 30min=0 / 6h=0** (DB `error_type like '%abs%'` 双窗口 0 rows 双重确认).
  连续多轮归零链: R1946=0 → ... → R1992=0 → R1994=0 → R1995=0.
- **fallback 5 FALLBACK-OK (0 真中断, 0 fallback 失败)**:
  全 5 条 75s `PRIMARY-FAIL-SKIP-CIRCUIT` (< chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层).
  趋势: R1967 8/R1970 7/.../R1990 3/R1991 4/R1992 5/R1994 6/R1995 5 — 区间内波动, 无恶化.
  全 5 条被 cc4101 在 75s 抢断切 ms, ms 救回: 4013/3485/1909/2725/3807ms (全 <5s) → **0 条 fallback 失败 → CC 收 0 真 502**.
  `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` = 0 → 0 真中断确认.
- **breaker**: cc4101 PRIMARY-BREAKER-OPEN 30min = 0; nv_gw 30min MS-FB-SERVED 5 条全 `state=CLOSED`
  (recorded failure 未达 5/300s 阈值仍 CLOSED, 非 NV-ANTH-BREAKER-FAIL); `grep -cE "BREAKER-FAIL|BREAKER.*OPEN"` = 0. breaker **OPEN 0 连续多轮**.
- **BUG-A 修复 (R1913) 真实生效**: 30min `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **5 次**, skip _try_tier_keys 第二轮省约 ~120s/fallback 请求.
  趋势: R1952 6/R1970 4/.../R1990 2/R1991 3/R1992 4/R1994 6/R1995 5 — 区间内波动持续触发, 长期生效.

## 决策: NOP 巡检轮 R103

- 数据全部满足"稳态": 30min SR>95%, 6h SR>95%, 502 全 NVCF 上游已知类, abs_cap 归零, fallback 全兜住 0 真中断, breaker 0 OPEN, BUG-A 持续触发.
- **冻结理由 (连续第 40 轮仍成立)**: 半成品未经 in-vivo 验证 (env NVU_GLM52_EXP_BACKOFF 不在容器 env) + 激活需同步 chain_budget 120→420 + cc4101 header 60→450 + post-200 软挂换 key 未实现 + 24h 观测窗口. 风险/收益不对等.
- 依用户诉求 "可以报错但不能让 cc2 中断" 仍达成 (R1995 0 真中断, 5 条 FALLBACK-OK 全被 ms_gw 兜住).
- 不改代码, 不 restart. 继续观测.

## 验证

- `curl -s http://localhost:40006/health`: nv_gw 健康 (StartedAt 13:33:43Z 维稳 R1933→R1995).
- `docker ps`: nv_gw / ms_gw / cc4101 / logs_db 全 Up. 0 restart.
- 0 改动 0 restart, 无需下窗口再验证 (NOP 巡检轮).

## 下一轮

- 继续 NOP 巡检 R104: 拉数据确认趋势. 重点看 6h SR 是否仍在 94-96% 区间 (非退化), 502 分类是否仍是 zombie/fbt/ATE 三类 (0 新可配置类), fallback 是否仍全 75s SKIP 被兜住 (0 fallback 失败 0 真中断), BUG-A SKIP-PEXEC2 是否持续触发.
- 任一指标恶化 (6h SR<94% 或 502 新类或 fallback 失败) 才考虑动.
- 轮号: 下一轮 git pull 看最新, peer HM1 可能已抢 R1996+ hm2_optimize_hm1 前缀; cc2 用 R1996 或更大 hm2_cc2 前缀不撞号.
- 若未来要解冻: 需先 in-vivo 验证 NVU_GLM52_EXP_BACKOFF (env 激活 + chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 同步), 且实现 post-200 软挂换 key, 再 24h 观测. 当前不动.
