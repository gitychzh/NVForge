# R2014 (HM2 cc2): NOP 巡检 R116 — 30min SR100%/6h SR96.38% 0 真中断, 连续冻结第 52 轮延续

> session: 2026-07-20 新 session (无上轮对话上下文, 由 STATE.md 接力)
> 模式: nv 直连 (cc4101→nv_gw), 指数退避+ms 双层方案半成品已入库冻结 (env NVU_GLM52_EXP_BACKOFF 不在 env=关, 从未 in-vivo 激活)
> 本轮: NOP 巡检 R116, 0 改动 0 restart

## 数据 (改前必有数据, 本 session 拉取, nv_gw StartedAt=2026-07-19T13:33:43Z 维 R1933)

### nv_gw 30min 窗口
- SR = 60/60 = **100%** (200:60 / 502:0)
- vs R2013 30min 98.55% (68/69, 502=1 zombie) → 本轮 100% (502=0): **30min 502 1→0 偏好 (那条 mid-stream zombie 本轮未再出现), 30min 小样本偏升非退化**
- 502 分类: 无 (0 rows)

### nv_gw 6h 窗口
- SR = 1011/1049 = **96.38%** (200:1011 / 502:38)
- vs R2013 6h 96.38% (1011/1049) → 本轮 6h **96.38% 完全持平 (+0.00pp), 大样本稳态区间 (R1960-2014 94.0-96.5% 区间内非退化)**
- 502=38 全已知三类 (**与 R2013 6h 502=38 完全持平, 0 重新分布**):
  - zombie_empty_completion ×24 (R2013=24 持平)
  - stream_first_byte_timeout ×8 (R2013=8 持平)
  - all_tiers_exhausted ×6 (R2013=6 持平)
  - **0 新可配置类**

### abs_cap
- 30min=0 / 6h=0 (DB `error_type like '%abs%'` 0 rows 双重确认; R1918 方案0 cap_origin 重置持续归零, 连续多轮: R1946=0 → ... → R2011=0 → R2013=0 → R2014=0)

### tier 30min error_type
- pexec_success ×47 / pexec_429 ×5 / pexec_conn_RemoteDisconnected ×4
- 全已知非配置类 (pexec_429 小样本波动非新可配置类; RemoteDisconnected 对应上游连接抖动)

### fallback (cc4101 30min)
- **7** FALLBACK-OK (0 真中断, 0 fallback 失败)
- vs R2013 fallback=5 → 本轮 7 (+2 区间内波动, 类别仍是已知类)
- 全 7 条 75s `PRIMARY-FAIL-SKIP-CIRCUIT` (< chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 preempt 层)
- **120s 跑满类本轮 0 持续趋稳归零** (R1951 4 → R1954 0 → ... → R2011 0 → R2013 0 → R2014 0; 全部 fallback 都在 75s SKIP 层被兜住, 无任何跑满 120s chain budget)
- 全 7 条被 cc4101 在 75s 抢断切 ms, ms 1403/2540/2786/3132/3877/4205/8042ms 救回 → **0 条 fallback 失败 → CC 收 0 真 502**
- `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` cc4101 30min = **0** → 确认 0 真中断
- 注: 日志见 `NV-MS-FB-SERVED ms_gw served ... (state=CLOSED)` 是 nv_gw 内部 ms_fb 路径记 breaker failure, state 仍 CLOSED, 非 NV-ANTH-BREAKER-FAIL 事件, 与 breaker OPEN 无关

### breaker
- cc4101 PRIMARY-BREAKER-OPEN 30min = **0**
- nv_gw 30min `grep -cE "BREAKER-FAIL|BREAKER.*OPEN|NV-ANTH-BREAKER-FAIL"` = **0**
  - vs R2013 nv_gw 30min=1 (`NV-ANTH-BREAKER-FAIL ... state=('CLOSED', 1, 0)`, req=b73daefc, 那条 mid-stream zombie 软挂) → 本轮 0: **那条 mid-stream zombie 本轮未再出现, breaker 无 recorded 事件, 偏好非恶化** (breaker OPEN 才是问题, recorded 但 CLOSED 是机制按设计吸收软挂; 本轮连 recorded 都无, 更稳)

### BUG-A 修复 (R1913) 在日志中真实生效确认
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **7 次**
- vs R2013=5 → 本轮 7 (区间内波动持续触发确认机制长期生效)

### 容器健康 + 参数
- `curl /health` = ok (passthrough, nv_num_keys=5, nvcf_pexec_models=[kimi_nv/dsv4p_nv/glm5_2_nv], default=dsv4p_nv)
- nv_gw StartedAt = **2026-07-19T13:33:43Z** (R1933→R2014 未再 restart, 维持)
- cc4101 StartedAt = **2026-07-19T12:10:22Z** (R1926→R2014 未再 restart, 维持)
- docker ps: cc4101 Up 13h / nv_gw Up 11h / ms_gw Up 2d / logs_db Up 3d (Up N = 容器创建时刻, 非 restart; 精确 StartedAt 以 docker inspect 为准)

## 本轮改动
**0 改动 0 restart** (NOP 巡检 R116).

## 决策依据 (为何不改)
全部稳态指标满足:
- 30min SR 100% > 94% 阈值 ✓
- 6h SR 96.38% 在 94-96.5% 稳态区间, vs R2013 +0.00pp 持平非退化 ✓
- 502 全 NVCF 上游已知三类 (zombie/stream_first_byte_timeout/all_tiers_exhausted), 0 新可配置类 ✓
- abs_cap 30min/6h 全归零 ✓
- fallback 全 75s SKIP 兜住, 0 fallback 失败 0 真中断 ✓
- breaker 未 OPEN (cc4101 PRIMARY-BREAKER-OPEN=0, nv_gw BREAKER=0) ✓
- BUG-A SKIP-PEXEC2 持续触发 (7 次) ✓

冻结理由 (连续第 52 轮) 仍成立:
- 半成品未经 in-vivo 验证 (env NVU_GLM52_EXP_BACKOFF 从未激活, 根本不在容器 env 中)
- 激活需同步 chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 + post-200 软挂换 key 未实现
- 24h 观测窗口
- 风险/收益不对等 (当前 6h SR96.38% 0 真中断, abs_cap 连续多轮归零, BUG-A 修复真实生效, 120s 跑满类持续趋零, 边际收益小)

任一指标恶化 (6h SR<94% 或 502 新类或 fallback 失败或 breaker 真 OPEN) 才考虑动.

## 验证
本轮 0 改动 0 restart, 无需验证 (health + docker ps 确认 nv_gw/cc4101 仍 R1933/R1926 原始 StartedAt 运行中).

## 下一轮建议
- 继续 NOP 巡检 (R117): 拉数据确认趋势, 重点看 6h SR 是否仍在 94-96.5% 区间, 502 是否仍全已知三类 (0 新可配置类), fallback 是否仍全 75s SKIP 兜住 (0 fallback 失败), BUG-A SKIP-PEXEC2 是否持续触发, breaker 是否仍未 OPEN.
- 轮号: 下轮 git pull 看最新, peer HM1 可能已抢 R2014+ hm2_optimize_hm1 前缀; cc2 用 R2015 或更大 hm2_cc2 前缀不撞号.
- 若未来要解冻: 需先 in-vivo 验证 NVU_GLM52_EXP_BACKOFF (env 激活 + chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 同步), 且实现 post-200 软挂换 key, 再 24h 观测. 当前不动.
