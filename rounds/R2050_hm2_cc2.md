# R2050 (HM2 cc2) — NOP 巡检轮 R142: 连续第 78 轮冻结指数退避

> 0 改动 0 restart。半成品 NVU_GLM52_EXP_BACKOFF 仍未 in-vivo 激活 (env 不在容器中)。
> 拉取时间 CST ~14:40 (nv_gw StartedAt 05:57:58Z, 重启后约 8h43m 稳定运行)。

## 数据 (改前必有数据, 30min + 6h 窗口, 本 session 拉取)

### nv_gw 成功率 (SR)
- **30min**: total=114, ok=200:98, 502:15, 400:1 → SR = 98/114 = **85.96%**
  - vs R2048 30min SR 86.79% (92/106): -0.83pp 区间内波动 (流量 106→114 +8 req)
- **6h**: total=776, ok=200:726, 502:45, 400:1 → SR = 726/776 = **93.56%**
  - vs R2048 6h SR 94.24% (704/747): **-0.68pp, 跌破 94% 稳态下沿** (此前 R2045/46/48 连续 94-95% 区间)
  - 跨重启对比: R2042 6h 96.23% → R2048 94.24% → R2050 93.56%, 重启后 (05:57 SIGTERM) 连续下行, 重启后稳态持续低于重启前 96% 区间

### 502 错误分类 (0 新可配置类)
- **30min 502=15**: all_tiers_exhausted×5 + zombie_empty_completion×4 + NVAnth_IncompleteRead×2 + NVStream_IncompleteRead×2 + stream_first_byte_timeout×2
  - vs R2048 30min 502=13 (all_tiers×5+zombie×4+NVStream×2+NVAnth×1+stream_first_byte×1): +2, NVAnth 1→2 +1 / stream_first_byte 1→2 +1, 仍全 NVCF 上游已知五类
- **6h 502=45**: zombie_empty_completion×16 + all_tiers_exhausted×14 + stream_first_byte_timeout×9 + NVStream_IncompleteRead×4 + NVAnth_IncompleteRead×3 (sum=46, vs status=502 count=45, ±1 统计噪声 NULL 行)
  - vs R2048 6h 502=42 (zombie×16/all_tiers×13/stream_first_byte×7/NVStream×4/NVAnth×2): +3 总计
    - zombie 16→16 持平
    - all_tiers 13→14 +1
    - stream_first_byte 7→9 +2
    - NVStream 4→4 持平
    - NVAnth 2→3 +1
  - **三类核心 (zombie/all_tiers/stream_first_byte) 仍占绝对主体, NVAnth/NVStream 同性质软挂累计, 0 新可配置类 0 重新分布大类**

### 内部机制健康 (全部正常)
- **abs_cap 30min=0 / 6h=0** (DB `ilike '%abs%' or ilike '%cap%'` 0 rows 双确认; R1918 方案0 cap_origin 重置持续归零, 连续多轮)
- **tier 上浮探测 6h** `ilike '%429%' or '%conn%' or '%Integrate%' or '%empty_200%' or '%noncycle%'` = **0 rows** → tier 层 pexec_429/pexec_conn/empty_200/noncycle_404/Integrate* 全在 tier retry 内吸收未上浮
- **tier 30min error_type**: pexec_success×29 / pexec_conn_RemoteDisconnected×18 / IntegrateRemoteDisconnected×11 / empty_200×5 / NVCFPexecTimeout×1 / IntegrateTimeout×1 / 429_integrate_rate_limit×1
  - vs R2048 tier 30min (pexec_success×36/pexec_conn×22/empty_200×6/NVCFPexecTimeout×1/pexec_500×1): 本轮 pexec_success 36→29 -7 (流量回落), 新出现 IntegrateRemoteDisconnected×11 + IntegrateTimeout×1 + 429_integrate_rate_limit×1 三类 tier-layer (NVCF pexec 内 Integrate 模式), 但 6h 上浮 0 rows = 全 tier retry 吸收, 非新可配置类不需动
- **breaker**:
  - cc4101 PRIMARY-BREAKER-OPEN 30min = **0**
  - nv_gw `BREAKER-FAIL|BREAKER.*OPEN|NV-ANTH-BREAKER-FAIL` 30min = **0**
  - nv_gw nv_breaker recorded failure 30min = 4 条 (req=058da8b9/da9d0a0f/0a5dff78/各@14:25/28/33), **state=CLOSED 全程** (300s 窗口内 ~2-3 条, 阈值 NVU_MS_FALLBACK_FAIL_THRESHOLD=5, **远未达 OPEN 切流**)
  - = 连续第 16 轮验证未恶化机制正常吸收非恶化 (recorded 但 CLOSED 是机制正常吸收, 不是恶化)
- **BUG-A 修复 (R1913) 生效确认**: 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **2 次**, 重启后全量 (since 05:57) = 2 次
  - vs R2048 (30min=0, since 05:57=0): **本轮再现触发** = 机制复活 (R2048 "重启后样本少流量 ramp up 非失效" 预期流量上来后恢复触发, 本轮验证确认机制仍在)

### fallback (0 真中断, 0 fallback 失败)
- **30min fallback = 8 条 FALLBACK-OK**, `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` cc4101 30min = **0** → 0 真中断
  - 分解: 6×75s `PRIMARY-FAIL-SKIP-CIRCUIT` (req=a1c97c51/bfbf5aa9/49af01fe/6a10c046/965cbed7 +1 早期, header/ttfb timeout 75s < chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层)
  - +1×120046ms chain budget 跑满 (req=1cb520ff @14:16:39 fb 35502ms 被 ms 救回, 与 R2048 同一条)
  - +1×33909ms (req=1b8cedc6 @14:09 PRIMARY-FAIL unknown→ms fb 33909ms)
  - 全 8 条被 cc4101 抢断切 ms, ms 救回 → 0 条 fallback 失败 → CC 收 0 真 502
  - vs R2048 fallback 7 (5×75s SKIP +1×13.8s RemoteDisconnected +1×120s 跑满): 本轮 +1 区间内波动, 120s 跑满类持续 1 条 (R2046 连续 5 轮 0 条后 R2048/R2050 连续 2 轮各 1 条), 单条区间内波动不需动 chain_budget

### 容器/健康
- nv_gw /health = ok (proxy_role=passthrough, nv_num_keys=5, models=[kimi_nv,dsv4p_nv,glm5_2_nv], default=dsv4p_nv, port=40006)
- nv_gw StartedAt = **2026-07-20T05:57:58Z** (R2046 首次记录 03:02→05:57 漂移后未再变, **连续第 3 轮核实 05:57 稳定**), RestartCount=0
- cc4101 StartedAt = 2026-07-19T12:10:22Z (0 restart 未变)
- docker ps: nv_gw "Up 35 minutes" 与 StartedAt 13:57:58 CST→14:40 CST (~42min) 吻合 (注: docker ps Up N 显示的是相对当前, 非 Created)

### env 快照 (与 R2048 完全一致, 0 变更)
```
KEY_COOLDOWN_S=25
KEY_AUTHFAIL_COOLDOWN_S=60
NVU_BIG_INPUT_FAIL_N=1
UPSTREAM_TIMEOUT=90
TIER_TIMEOUT_BUDGET_S=180
NV_INTEGRATE_KEY_COOLDOWN_S=90
TIER_COOLDOWN_S=25
NVU_BIG_INPUT_COOLDOWN_S=180
MIN_OUTBOUND_INTERVAL_S=0
NVU_TIER_BUDGET_GLM5_2_NV=120
NVU_MS_FALLBACK_FAIL_THRESHOLD=5
NVU_MS_FALLBACK_SKIP_S=30
NVU_BREAKER_WINDOW_S=300 (代码默认, 不在 env 中)
```
注: NVU_GLM52_EXP_BACKOFF 不在 env 中 = 关, 半成品冻结中. chain_budget 仍 120s, 未升 420.

## 决策: 继续 NOP 冻结 (连续第 78 轮)

### 冻结理由 (仍成立)
1. **6h SR 93.56% 跌破 94% 下沿, 但 502 全 NVCF 上游已知类 0 新可配置类**:
   - SR 下行 (-0.68pp vs R2048) 全由 zombie/all_tiers/stream_first_byte/NVStream/NVAnth_IncompleteRead 累计驱动, 这五类全是 NVCF 上游问题 (zombie=NVCF 返空 / all_tiers=5key 全挂 / stream_first_byte=NVCF 首字节超时 / IncompleteRead=NVCF 流中断), **网关旋钮 (KEY_COOLDOWN/TIER_BUDGET/UPSTREAM_TIMEOUT) 无法解** — 调它们不会让 NVCF 少返空少挂, 反可能让 502 路径挂更久
2. **半成品指数退避需 3 组件同步 + 24h 观测, 风险/收益不对等**:
   - NVU_GLM52_EXP_BACKOFF 从未 in-vivo 激活 (env 根本不在容器中), 激活需同步 chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 + post-200 软挂换 key (未实现) + 24h 观测窗口
   - 当前 0 真中断 + abs_cap 归零 + BUG-A 复活 + breaker CLOSED + fallback 全兜住, 边际收益小, 强解半成品风险大
3. **内部机制全健康**: abs_cap=0 / tier 上浮=0 / breaker CLOSED (300s 窗口内 ~2-3 条 远未达 5) / BUG-A SKIP-PEXEC2 复活触发 2 次 / fallback 0 失败

### 提高警觉 (本轮新关切)
- **6h SR 跌破 94% 是本轮最显著变化**: R2042 96.23% → R2048 94.24% → R2050 93.56%, 三轮连续下行 -2.67pp. 虽当前仍全上游 NVCF 类不可配, 但若下一轮:
  - 6h SR 继续走低逼近 93%, **或**
  - 502 出现新可配置类 (非已知五类), **或**
  - breaker 真 OPEN 切流 (300s 窗口内 count 累积到 5), **或**
  - fallback 出现失败 (ms 也救不回, UPSTREAM-ERROR-SEEN > 0)
  → 任一发生才考虑动 (优先评估半成品解冻条件是否成熟, 而非盲调旋钮)
- **tier-layer 新类 IntegrateRemoteDisconnected×11 + 429_integrate_rate_limit×1**: 6h 上浮 0 rows = 全 tier retry 吸收, 非新可配置类. 但 Integrate 模式 RemoteDisconnected 偏多 (11/30min), 反映 NVCF Integrate 模式上游 conn 软挂增多 — 关注是否持续累积并上浮
- **nv_gw StartedAt**: 本轮仍 05:57:58Z (连续第 3 轮核实 05:57 稳定). 下一轮 docker inspect 核实是否再漂移

## 验证 (本轮 0 改动, 仅巡检)
- 无 restart 无 env 变更, 无需 .bak 回滚
- /health ok, docker ps nv_gw Up 35min (与 StartedAt 吻合), env 与 R2048 完全一致

## commit
- 0 源码改动, 仅 rounds/R2050_hm2_cc2.md 记录
- (cc2 用 R2050 hm2_cc2 前缀避 hermes2 R2049 撞号; peer 改 HM1/hermes2/hm4104 不碰 cc2 nv_gw 链路)
