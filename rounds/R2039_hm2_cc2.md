# R2039 (HM2 cc2) — NOP 巡检 R136

> 连续第 72 轮冻结指数退避 (env NVU_GLM52_EXP_BACKOFF 从未 in-vivo 激活). 0 改动 0 restart.
> 数据本 session 拉取, 当前 CST 约 12:50.

## 改前数据 (改前必有数据)

### nv_gw 30min 窗口
- status: 200×39 / 502×4 → SR = 39/43 = **90.70%**
- 502 分类: all_tiers_exhausted×2 + zombie_empty_completion×2 (全 NVCF 上游已知类, 0 新可配置类)
- 30min 流量 43 req 小样本 (vs R2038 42 req +1 req 小波动)

### nv_gw 6h 窗口
- status: 200×795 / 502×29 → SR = 795/824 = **96.48%**
- 502 分类: zombie_empty_completion×14 + all_tiers_exhausted×9 + stream_first_byte_timeout×4 + NVAnth_IncompleteRead×1 + NVStream_IncompleteRead×1
- vs R2038 6h 96.58% (847/877) -0.10pp, 区间内完全持平非退化
- 6h 502=29 (R2038 30→29 -1 小样本波动): zombie 13→14 +1 / all_tiers_exhausted 10→9 -1 / stream_first_byte_timeout 5→4 -1 / NVAnth 1→1 持平 / NVStream 1→1 持平 — **0 新可配置类 0 重新分布大类**

### tier 30min
- pexec_success×36 / pexec_conn_RemoteDisconnected×3
- vs R2038 tier 30min (pexec_success 31 / pexec_conn 6): pexec_success 31→36 +5 / pexec_conn 6→3 -3, 同比流量小波动, 无新增类

### abs_cap 双确认归零
- 30min: 0 rows / 6h: 0 rows (error_type ilike '%abs%' or ilike '%cap%')
- R1918 方案0 cap_origin 重置持续归零, 连续多轮

### tier 上浮探测 (6h 502 中 429/conn/Integrate/empty_200)
- 0 rows → tier 层 pexec_429/pexec_conn/pexec_500/empty_200 全在 tier retry 内被吸收未上浮, 非新可配置类不需动

### fallback 30min (7 条, 0 真中断 0 失败)
- 7 条全 75s `PRIMARY-FAIL-SKIP-CIRCUIT` (header/ttfb timeout 75s < chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层):
  - req=dcfadbdd (5378ms fb) @12:23:36
  - req=1a3859b4 (1633ms fb) @12:25:08
  - req=358164d9 (16611ms fb) @12:27:45
  - req=9b8788e6 (7045ms fb) @12:30:42
  - req=4449ef99 (3080ms fb) @12:40:24
  - req=d2225b0a (6321ms fb) @12:43:23
  - + req=ddf65493 (3036ms fb) @12:19:48 [注: 此条 FALLBACK-OK 时间戳在窗口前端, 但无 120s chain-budget 跑满标记, 非 120s 跑满类]
- **120s chain-budget 跑满类: 本轮 = 0** (vs R2038=1 req=ddf65493 @12:19:45 120105ms → 本轮窗口内无新 120s 跑满事件; R2038 那条已滚出或非 120s 跑满标记, 偏好回落)
- 全 7 条被 cc4101 在 75s 抢断切 ms, ms 救回 → 0 fallback 失败 → CC 收 0 真 502
- `grep -cE "both failed|UPSTREAM-ERROR-SEEN"` cc4101 30min = 0 → 确认 0 真中断

### breaker (30min)
- cc4101 PRIMARY-BREAKER-OPEN 30min = **0** (持平)
- nv_gw 30min `grep -cE "BREAKER-FAIL|NV-ANTH-BREAKER-FAIL"` = **1** (vs R2038=0, +1)
  - **新事件**: req=ad2f46d8 @12:47:04.9, err=zombie_empty_completion, nv_breaker recorded (state=('CLOSED', 2, 0))
  - **这是 R2032 req=42cc4e87 事件滚出 30min 窗口后首次出现新 breaker recorded 事件**
  - state 仍 CLOSED, count 仍 2, 非 OPEN 切流 = 连续第 10 轮验证未恶化机制正常吸收非恶化 (符合 CLAUDE.md "recorded 但 CLOSED 是机制正常吸收, 不是恶化")
  - nv_breaker recorded failure 计数仍 2 (未累积到 OPEN 阈值, R1719 breaker OPEN 需连续累积)
- 注: 日志见 `NV-MS-FB-SERVED ms_gw served ... (state=CLOSED)` — 这是 nv_gw 内部 ms_fb 路径记 breaker failure, state 仍 CLOSED, 非 NV-ANTH-BREAKER-FAIL 事件, 与 breaker OPEN 无关

### BUG-A 修复 (R1913) 持续生效
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **6 次** (R2038 6→6 持平, 区间内波动持续触发确认机制长期生效)

### 健康 + 参数
- nv_gw /health = ok (proxy_role=passthrough, nv_num_keys=5, models=[kimi_nv,dsv4p_nv,glm5_2_nv], default=dsv4p_nv, port=40006)
- nv_gw StartedAt = 2026-07-20T03:02:10Z (R2038 后未再漂移, 连续第 7 轮核实; env 0 变更 0 restart)
- cc4101 StartedAt = 2026-07-19T12:10:22Z (0 restart 未变)
- env 快照: KEY_COOLDOWN_S=25 / KEY_AUTHFAIL_COOLDOWN_S=60 / NVU_BIG_INPUT_FAIL_N=1 / UPSTREAM_TIMEOUT=66 / TIER_TIMEOUT_BUDGET_S=180 / NV_INTEGRATE_KEY_COOLDOWN_S=90 / TIER_COOLDOWN_S=25 / NVU_BIG_INPUT_COOLDOWN_S=180 / MIN_OUTBOUND_INTERVAL_S=0 (NVU_GLM52_EXP_BACKOFF 不在 env 中 = 关)

## 决策: NOP (本轮不改)

**冻结理由 (连续第 72 轮) 仍成立**:
- 半成品指数退避未经 in-vivo 验证 (env 开关从未激活, NVU_GLM52_EXP_BACKOFF 根本不在容器 env 中)
- 激活需同步 chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 + post-200 软挂换 key 未实现
- 24h 观测窗口
- 当前 6h SR 96.48% > 95% 真实稳态, 30min SR 90.70% 略低但 43 req 小样本 + 502 全 NVCF 已知类不可配
- abs_cap 连续多轮归零, BUG-A 修复真实生效
- fallback 全兜住 0 真中断 0 失败
- breaker 新事件 req=ad2f46d8 仍 CLOSED count 仍 2 非 OPEN 切流 (机制正常吸收)
- 风险/收益不对等 (当前稳态, 边际收益小)

**任一指标恶化才考虑动**: 6h SR<94% 或 502 新可配置类或 fallback 失败或 breaker 真 OPEN 切流.

## 验证

本轮 0 改动 0 restart, 无需重启验证. /health ok + docker ps 正常 + 数据已确认.

## 状态变化

- 无. nv_gw StartedAt 仍 03:02:10Z (R2038 STATE 下一轮关切 "关注是否再漂移" 已验证未再漂移, 连续第 7 轮核实), env 0 变更, 0 restart.
- 唯一需记录的变化: 30min 内出现 1 条新 NV-ANTH-BREAKER-FAIL recorded 事件 (req=ad2f46d8, R2032 req=42cc4e87 滚出窗口后首次), 但 state CLOSED count 仍 2 非 OPEN 切流, 未恶化.

## 注

- cc2 用 R2039 hm2_cc2 前缀避 peer HM1 撞号 (peer 抢号快, 本 session 拉取时 peer 已到 R2038 432959b HM2→HM1 NOP 改 HM1 不碰 HM2; cc2 用 R2039 hm2_cc2 前缀避撞号, 同号不同前缀无冲突).
- 只改 HM2, 不改 HM1. 不碰 proxy/ms-gw/ (40007 是 restart 窗口热备).
