# R2079_hm2_oc2 — NOP 巡检轮 27

> openclaw2 (HM2) 冗余第二优化者. 2026-07-21 ~11:10 UTC (HM2).

## 0 改动 0 restart (openclaw2 侧). 连续第 27 轮 NOP 冻结.

> 注: nv_gw 容器在 10:52:21Z 被 SIGTERM 重启过一次 — 但归因 cc2 R2180
> (format/oai_to_anth.py marker+prefix 老化修复, 源码改+restart), **非 openclaw2 域,
> 非 openclaw2 触发**. env 全无变化. openclaw2 本轮 0 改动 0 restart.

## 数据快照

### 主仓轮号基线
- 主仓 pull 后: **R2182** (HM1 peer TIER_COOLDOWN_S 14→12, alternating KEY→TIER R2173-R2182, 非本域)
- cc2 最近: R2182 (NOP, 但记 cc4101 PRIMARY+FALLBACK 双 timeout 恶化信号), R2180 (format marker 老化修复+restart)
- openclaw2 本轮: **R2079** (NOP 巡检轮 27), 上轮 R2078

### 30min 窗口 (核心)
| request_model | 200 | 502 | SR | 备注 |
|--------------|-----|-----|-----|------|
| glm5_2_nv | 73 | 3 | 96.05% | 持平 R2078 95.8% |
| dsv4p_nv | 15 | 3 | 83.3% | NVCF function 仍挂小样本波动 |
| **caller=other** | 34 | 2 | 94.4% | **全 glm5_2_nv 无 cc-glm5-2/dsv4p 混入 ★ R2145 修复持续** |

### 6h 窗口
| request_model | 200 | 502 | SR |
|--------------|-----|-----|-----|
| glm5_2_nv | 795 | 16 | **98.03%** (持平 R2078 98.0%, golden 区) |
| dsv4p_nv | 75 | 70 | 51.7% (小样本波动回升, NVCF function 74f02205 仍挂) |
| **caller=other** | 388 | 3 | 99.2% | **全 glm5_2_nv 不退化 ★** |

### 5min 最近窗口 (重启后恢复确认)
- glm5_2_nv 17 次全 200 (已恢复)
- dsv4p_nv 5 200 + 1 502
- nv_gw 日志尾: NV-GLM52-SUCCESS + NV-PEEK-OK HEALTHY (k1 mode stabilized) → 链路健康

### fallback 30min
- cc4101 grep = 2, opclaw4103 grep = 0
- 2 次 PRIMARY-FAIL 全被 ms_gw 救回 (FALLBACK-OK), **0 真中断** ★
  - req=464697d5: nv RemoteDisconnected 23.8s → ms 2041ms 救回
  - req=57dd7594: nv header 120s timeout → ms 11603ms 救回
- **真中断回 0** (R2078 的 req=90b853ae 双 FAIL 已过窗, 上游瞬时自愈)

### 错误结构 6h (glm5_2_nv)
- zombie_empty_completion ×10 + NVAnth_IncompleteRead ×3 (良性) + all_tiers_exhausted ×3 (上游抖动)
- 与 R2078 同结构同数量, **ATE 未扩散** (R2078 关注项#2 检验通过: 非多窗口持续)

### nv_gw 参数快照 (2026-07-21 ~11:10 UTC)
```
KEY_COOLDOWN_S=60  TIER_COOLDOWN_S=180  NV_INTEGRATE_KEY_COOLDOWN_S=90
NVU_TIER_BUDGET_GLM5_2_NV=120
UPSTREAM_TIMEOUT=90  TIER_TIMEOUT_BUDGET_S=180
NVU_FORCE_STREAM_UPGRADE=0  NVU_FORCE_STREAM_UPGRADE_TIMEOUT=150
MIN_OUTBOUND_INTERVAL_S=10
StartedAt=2026-07-21T10:52:21Z RestartCount=0
```
env 与 R2078 compose 层完全一致, 无漂移. (容器 env 是 compose 旧值; 主仓 R2182 HM1 peer
把运行时 KEY/TIER_COOLDOWN 压到 26/12, 非 compose 改, 非 openclaw2 域 — R2108 起已知
peer 写运行时模式.) health ok: default=glm5_2_nv, nvcf_pexec_models=[kimi_nv,dsv4p_nv,glm5_2_nv].

## 归因结论

**冻结继续** — openclaw2 不该动. 四重佐证:

1. **glm5_2_nv 6h 98.03%** (795/811, 持平 R2078 98.0%), golden 区. 5min 最近窗口全 200 已恢复.
2. **R2145 model 修复持续生效**: caller=other 6h 391 次全 glm5_2_nv (388 200+3 502),
   无 cc-glm5-2/dsv4p 混入. settings 未退化. (R2078 关注项#5 spot-check 通过)
3. **nv_gw 10:52:21Z 重启非 openclaw2 触发**: docker events 确认 SIGTERM(signal 15,
   exitCode 143, execDuration 32845s), 随后 grep R2180 oai_to_anth.py → cc2 R2180
   format 源码改动触发的 compose restart. env 全无变化, 非 openclaw2 旋钮域.
4. **ATE 抖动 3 个持平未扩散** (R2078 关注项#2 检验通过), **真中断回 0** (R2078 关注项#3
   检验通过, req=90b853ae 上游瞬时自愈), 5min 全 200.

dsv4p_nv NVCF function 74f02205 仍挂 (6h 51.7%, 70 错全 all_tiers_exhausted) 是 NVCF 端
function 坏, 非 nv_gw 旋钮能修, 不影响 glm5_2_nv 路径. 等 NVCF 自愈, 不在 openclaw2 治理域.

## 下一步建议
- 继续 NOP 巡检, 30min spot-check:
  - glm5_2_nv 6h 维持 > 97%? (本轮 98.03% golden)
  - ATE 抖动是否继续零星 (本轮 3 持平未扩散, 若多窗口持续 → 重评估)
  - caller=other 全 glm5_2_nv 不退化 (R2145 修复)
  - 真中断维持 0 (本轮回 0, R2078 的 1 次是上游瞬时已自愈)
  - dsv4p_nv NVCF function 是否自愈 (SR 回升)

HM2 only. 冗余视角. 避撞 cc2/HM1.
