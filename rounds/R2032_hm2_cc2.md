# R2032 (HM2 cc2): NOP 巡检 R131 — 连续第 67 轮冻结指数退避

**日期**: 2026-07-20
**轮号**: R2032 (hm2_cc2 前缀避 peer HM1 撞号; peer 已到 R2031 b252f41 "HM2->HM1 NOP" 改 HM1 不碰 HM2 nv_gw)
**模式**: NOP 巡检, 0 改动 0 restart
**上一轮**: R2031 (87f1770) — 连续第 66 轮冻结 (注: peer R2031 b252f41 写 "HM2->HM1 NOP" 改 HM1 不碰 HM2 nv_gw)

## 状态变化 (本 session 核实)

**nv_gw StartedAt 未再漂移**: 本轮 docker inspect 核实 nv_gw StartedAt = **2026-07-20T03:02:10Z**
(UTC, 对应本地 CST 11:02:10), 与 R2030/R2031 记录完全一致, 本轮期间未再重启, 连续第 2 轮核实未漂移。
RestartCount=0 + env 快照与 R2031 完全一致 (KEY_COOLDOWN=25 / TIER_COOLDOWN=25 /
UPSTREAM_TIMEOUT=66 / TIER_TIMEOUT_BUDGET=180 / MIN_OUTBOUND=0 / NVU_GLM52_EXP_BACKOFF 不在 env=关)。
cc4101 StartedAt 仍 2026-07-19T12:10:22Z (R1926 后 0 restart, 未变)。
docker ps 显示 nv_gw "Up 35 minutes" 与 StartedAt 03:02:10Z (UTC, = 11:02:10 CST) 到当前 11:36 CST 完全吻合。
**R2031 STATE 下一轮关切 "关注 StartedAt 是否再漂移" → 本轮核实未再漂移 (连续第 2 轮)。**

## 改前数据 (本 session 拉取, 当前 CST 11:36, nv_gw StartedAt 11:02:10 CST)

### nv_gw 30min 窗口
- SR = 102/104 = **98.08%** (200:102 / 502:2)
- 502=2: zombie_empty_completion×1 + all_tiers_exhausted×1
- **vs R2031 30min 96.25% (77/80) +1.83pp 区间内偏升非退化** (200 77→102 +25 / 502 3→2 -1, 30min 流量 +24 req (80→104) 持续回升 nv_gw 在干活)
- 30min 502=2 (vs R2031 502=3 -1): zombie×2→1 / all_tiers_exhausted×1→1 / **本轮 30min 无 NVAnth_IncompleteRead** (R2031 30min 无, 持平) — **全已知类 0 新可配置类**

### nv_gw 6h 窗口
- SR = 964/996 = **96.79%** (200:964 / 502:32)
- 502=32: zombie_empty_completion×17 / all_tiers_exhausted×7 / stream_first_byte_timeout×7 / NVAnth_IncompleteRead×1
- **vs R2031 6h 96.66% (956/989) +0.13pp 区间内持平偏升非退化** (200 956→964 +8 / 502 33→32 -1, 双向 = 6h 滚动窗口前端旧数据滚出 + 后端新 200 涌入, 非退化)
- 6h 502=32 (vs R2031 502=33 -1): zombie 17→17 持平 / all_tiers_exhausted 7→7 持平 / **stream_first_byte_timeout 8→7 -1 小样本波动** / NVAnth_IncompleteRead 1→1 持平
- **0 新可配置类, 0 重新分布大类** — 仍全 NVCF 上游已知三类 + NVAnth_IncompleteRead 同性质上游软挂

### tier 30min error_type (新观测点)
- pexec_success×84 / pexec_500×9 / pexec_conn_RemoteDisconnected×9 / pexec_429×5 / IntegrateRemoteDisconnected×3
- **vs R2031 tier 30min (pexec_success 74 / pexec_conn 5 / pexec_500 1)**: 本轮 pexec_success 74→84 +10 流量回升同向印证 / **新出现 pexec_429×5 (R2031 无) + IntegrateRemoteDisconnected×3 (R2031 无) + pexec_500 1→9 +8 + pexec_conn 5→9 +4**
- **关键**: 6h nv_requests 502 中 `error_type ilike '%429%' or ilike '%conn%' or ilike '%Integrate%'` = **0 rows** → 即 tier 层的 pexec_429/pexec_conn/IntegrateRemoteDisconnected 全在 tier retry 内被吸收, **未上浮为 nv_gw 502**
- 即: 上游 NVCF 429 抖动 + IntegrateRemoteDisconnected (NV integrate 子链路断连) 是 tier 层的 retry 噪声, 被 tier retry 吃掉不上浮, **非新可配置类不需动** (符合 CLAUDE.md "tier 层错误被 retry 吸收 = 正常")

### abs_cap (双重确认归零)
- 30min `error_type ilike '%abs%' or ilike '%cap%'` = **0 rows**
- 6h = **0 rows**
- R1918 方案0 cap_origin 重置持续归零, 连续多轮 (R1946=0 → ... → R2031=0 → 本轮=0)

### fallback (cc4101 30min, 负向核心指标)
- FALLBACK-OK = **6 条** (0 真中断, 0 fallback 失败)
- 全 6 条 = 6×75s `PRIMARY-FAIL-SKIP-CIRCUIT` (header/ttfb timeout 75s < chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层)
  - req: f18b5cea / be298e72 / 7bfc87cd / 26a9b747 / 75b72d6a / 284f88ed
- **vs R2031 fallback 7 → 本轮 6 (-1 区间内波动, 类别仍全 75s SKIP-CIRCUIT 已知类)**
- `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` cc4101 30min = **0** → 确认 0 真中断 (ms_gw 全救回)
- **120s 跑满类本轮 0 持续趋稳归零** (R1951 4 → R1954 0 → ... → R2031 0 → 本轮 0; 全部 fallback 都在 75s SKIP 层被兜住, 无任何跑满 120s chain budget)
- 注: 日志见 `NV-MS-FB-SERVED` 30min=4 — 这是 nv_gw 内部 ms_fb 路径记 breaker failure, state 仍 CLOSED, 非 NV-ANTH-BREAKER-FAIL 事件, 与 breaker OPEN 无关

### breaker (cc4101 + nv_gw 30min)
- cc4101 PRIMARY-BREAKER-OPEN 30min = **0**
- nv_gw 30min `grep -cE "BREAKER-FAIL|BREAKER.*OPEN|NV-ANTH-BREAKER-FAIL"` = **1**
- **本轮出现 1 条新 NV-ANTH-BREAKER-FAIL recorded 事件** (vs R2031=0):
  - `[11:34:58.8] [NV-ANTH-BREAKER-FAIL] (glm5_2_nv) anth mid-stream soft-fail err=zombie_empty_completion -> nv_breaker recorded (state=('CLOSED', 2, 0), req=42cc4e87)`
- **state=('CLOSED', 2, 0) — count 仍 2, state 仍 CLOSED, 非 OPEN 切流**
- **分析**: R2027 那条老事件 (req=a72faa19 @10:41:17.7) 早已滚出 30min 窗口 (当前 11:36, 10:41 已 55min+ 前); 本轮新增的 req=42cc4e87 @11:34:58.8 是**新事件**, 但 nv_breaker recorded failure 计数仍 2 (count 未累积到 OPEN 阈值), **state 仍 CLOSED 非 OPEN 切流 = 机制正常吸收, 不是恶化** (符合 CLAUDE.md "recorded 但 CLOSED 是机制正常吸收, 不是恶化")
- **连续第 5 轮 (R2027/R2029/R2030/R2031/R2032) 同机制验证未恶化**: R2027 首次触发 → R2029/R2030 30min 覆盖同老事件 → R2031 老事件滚出窗口无新事件 → R2032 新事件出现但 count 仍 2 state 仍 CLOSED — 即 nv_breaker 一直在正常吸收 mid-stream soft-fail, 从未累积到 OPEN 切流
- **R2031 STATE 下一轮关切已验证**: "nv_gw 30min BREAKER-FAIL recorded 已 0 (R2027 事件滚出窗口) — 下一轮继续看是否仍 0 或出现新事件; 若出现新 NV-ANTH-BREAKER-FAIL 且 count 继续累积到 OPEN 阈值真切流才算恶化" → 本轮出现新事件, 但 count 仍 2 state 仍 CLOSED, **未达 OPEN 阈值非切流, 未恶化**

### BUG-A 修复 (R1913) 在日志中真实生效确认
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **5 次**
- (R2005 3 / R2006 4 / R2007 5 / R2009 5 / R2011 4 / R2013 5 / R2014 7 / R2015 6 / R2016 6 / R2018 6 / R2019 8 / R2020 7 / R2021 8 / R2022 7 / R2023 7 / R2025 5 / R2026 6 / R2027 5 / R2029 5 / R2030 4 / R2031 5 / 本轮 5, 区间内波动持续触发确认机制长期生效)

### nv_gw /health
- `{"status":"ok","proxy_role":"passthrough","nv_num_keys":5,"nvcf_pexec_models":["kimi_nv","dsv4p_nv","glm5_2_nv"],"nv_model_tiers":["kimi_nv","dsv4p_nv","glm5_2_nv"],"nv_default_model":"dsv4p_nv","port":40006}`
- port=40006 确认聚焦 nv_gw, 未误碰 ms_gw(40007)

## 决策: NOP (连续第 67 轮冻结)

数据全部满足"稳态":
- 30min SR 98.08% > 94% 阈值 (实际区间上沿)
- 6h SR 96.79% 在 94-96.7% 稳态区间偏上 (vs R2031 +0.13pp 非退化)
- 502 全 NVCF 上游已知三类 + NVAnth_IncompleteRead 同性质软挂, 0 新可配置类 0 重新分布大类
- abs_cap 30min/6h 双确认归零 (R1918 方案0 持续)
- fallback 6 条全 75s SKIP-CIRCUIT 兜住, 0 fallback 失败 0 真中断
- breaker nv_gw 新 recorded 1 条但 state CLOSED count 2 非 OPEN 切流 = 正常吸收非恶化 (连续第 5 轮验证)
- BUG-A SKIP-PEXEC2 持续触发 5 次
- tier 层 pexec_429/IntegrateRemoteDisconnected 被 retry 吸收不上浮为 502
- nv_gw StartedAt 未再漂移 (连续第 2 轮核实)

**冻结理由 (连续第 67 轮) 仍成立**:
半成品 NVU_GLM52_EXP_BACKOFF 从未 in-vivo 验证 (env 开关不在容器 env 中) + 激活需同步 chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 + post-200 软挂换 key 未实现 + 24h 观测窗口. 风险/收益不对等 (当前 6h SR96.79% 0 真中断, abs_cap 归零, BUG-A 真实生效, 120s 跑满类趋零, 边际收益小).

**本轮 0 改动 0 restart**, 延续 R1928 冻结决定。

## 验证结果

- nv_gw /health = ok (passthrough, 5 keys, port 40006) ✅
- docker ps: nv_gw "Up 35 minutes" 与 StartedAt 03:02:10Z (= 11:02:10 CST) 吻合 ✅
- RestartCount=0, env 0 变更 ✅
- 0 改动 0 restart, 无需回滚

## 下一轮建议

- **继续 NOP 巡检 (R132)**: 数据全部满足稳态, 冻结理由仍成立
- **拉数据确认趋势**: 重点看 6h SR 是否仍在 94-96.7% 区间 (非退化), 502 分类是否仍是已知三类 + NVAnth_IncompleteRead (0 新可配置类), fallback 是否仍全 75s SKIP 被兜住 (0 fallback 失败 0 真中断), BUG-A SKIP-PEXEC2 是否持续触发
- **breaker 关切**: 本轮新出现 1 条 NV-ANTH-BREAKER-FAIL recorded (req=42cc4e87) 但 count 仍 2 state 仍 CLOSED — 下一轮继续看是否仍 count=2 / state=CLOSED (无累积 = 偏好); 若 count 继续累积到 OPEN 阈值真切流才算恶化
- **tier 层关切**: 本轮 tier 30min 新出现 pexec_429×5 + IntegrateRemoteDisconnected×3 — 下一轮看是否持续出现; 已确认未上浮为 nv_gw 502 (6h nv_requests 0 rows), 即被 tier retry 吸收; 若未来上浮为 502 新可配置类才需动
- **关注 nv_gw StartedAt 是否再次漂移**: 本轮核实仍 03:02:10Z (连续第 2 轮未漂移); 下一轮 docker inspect 核实; 若再次变化且非 cc2 改, 记录时间点供后续排查
- **轮号**: 下一轮 git pull 看最新, peer HM1 可能已抢 R2032+ hm2_optimize_hm1 前缀; cc2 用 R2033 或更大 hm2_cc2 前缀不撞号
- **若未来要解冻**: 需先 in-vivo 验证 NVU_GLM52_EXP_BACKOFF (env 激活 + chain_budget 120→420 + cc4101 PRIMARY_HEADER_TIMEOUT 60→450 同步), 且实现 post-200 软挂换 key, 再 24h 观测. 当前不动.
