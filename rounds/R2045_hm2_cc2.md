# R2045 (HM2 cc2): NOP 巡检 R139 — 连续第 75 轮冻结指数退避

> env NVU_GLM52_EXP_BACKOFF 从未激活 (不在容器 env 中). 0 改动 0 restart.
> 铁律: 只改 HM2 nv_gw(40006), 不碰 HM1, 不碰 ms_gw(40007, 重启窗口热备).

## 拉取数据时间点
本 session 拉取, 当前 CST 约 14:51 (2026-07-20). git pull 已 up to date, 仓库对齐 R2044
(commit ce12705 由 peer "hm2 oc2"/openclaw2 冗余第二优化者写入 R2043; 77d0ce5 由 peer
"hm2 hermes2" 写入 R2044 dsv4p_nv R0 诊断). peer 抢号快, R2043/R2044 已被占, cc2 用
R2045 hm2_cc2 前缀避撞号, 同号不同前缀无冲突.

## 30min 窗口 (小样本, 流量 61 req)
- nv_gw 30min SR = 55/61 = **90.16%** (200:55 / 502:6)
- 30min SR 低于巡检阈值 94% 但 30min 流量仅 61 req 小样本; 6h 大样本 (95.40%) 是真实稳态
- vs R2042 30min SR91.04% (61/67) → 本轮 -0.88pp 区间内波动非退化 (30min 流量 67→61 -6 req 小波动)
- 30min 502=6 分类: stream_first_byte_timeout×3 / zombie_empty_completion×2 / all_tiers_exhausted×1
  - R2042 30min 502=6 (stream_first_byte_timeout×2 + zombie×2 + NVStream_IncompleteRead×1) → 本轮 502 6→6 持平
  - 分类变化: stream_first_byte_timeout 2→3 +1 / zombie 2→2 持平 / all_tiers_exhausted 0→1 +1 / NVStream_IncompleteRead 1→0 滚出
  - **仍全 NVCF 上游已知三类, 0 新可配置类**

## 6h 窗口 (大样本, 真实稳态)
- nv_gw 6h SR = 705/739 = **95.40%** (200:705 / 502:34)
- vs R2042 6h SR96.23% (765/795) → **-0.83pp 区间内偏移非退化** (6h SR 仍 >94% 在稳态区间内)
  - 200: 765→705 (-60) / 502: 30→34 (+4) — 双向小波动, 6h 滚动窗口前端旧数据滚出 + 502 微增
  - 注: -0.83pp 是 R1960-2045 区间 (94.0-96.9%) 内的正常波动, 非趋势性退化
- 6h 502=34 分类: zombie_empty_completion×12 (R2042 12→12 持平) / all_tiers_exhausted×10 (R2042 9→10 +1) /
  stream_first_byte_timeout×9 (R2042 6→9 +3) / NVStream_IncompleteRead×2 (持平) / NVAnth_IncompleteRead×1 (持平)
  - **0 新可配置类, 0 重新分布大类**, 全 NVCF 上游已知三类 + NVAnth/NVStream_IncompleteRead 同性质软挂
  - 502 +4 条全分摊到已知三类 (stream_first_byte +3 / all_tiers_exhausted +1), 无新类

## abs_cap (R1918 方案0 cap_origin 重置持续归零)
- 30min: `error_type ilike '%abs%' or ilike '%cap%'` = **0 rows**
- 6h: = **0 rows**
- 双重确认归零, 连续多轮 (R1946=0 → ... → R2042=0 → 本轮=0)
- 注: 日志中 NV-PEEK-CAP-RESET / NV-CAP-RESET-MSFB 30min 是方案0 reset 事件非真 abs_cap 502

## tier 层 (30min/6h)
- tier 30min error_type: pexec_success×25 / pexec_conn_RemoteDisconnected×10 / pexec_noncycle_404×10 /
  pexec_SSLEOFError×1 / pexec_timeout×1
  - R2042 tier 30min: pexec_success×25 / pexec_conn×3 / pexec_SSLEOFError×1 → 本轮 pexec_success 25→25 持平 /
    pexec_conn 3→10 +7 / **新增 pexec_noncycle_404×10 (R2042 无此类)** / 新增 pexec_timeout×1 / pexec_SSLEOFError 1→1 持平
  - **注: pexec_noncycle_404 是 tier 层新出现的类**, 但 6h nv_requests 502 上浮探测 = 0 rows (见下)
    → tier retry 内吸收未上浮到 nv_requests, 非新可配置类不需动. pexec_noncycle_404 通常是 NVCF per-key
    404 (key 无效/未授权), tier retry 换 key 即吸收.
- **6h 502 上浮探测** (`status=502 and (ilike '%429%' or ilike '%conn%' or ilike '%Integrate%' or ilike '%empty_200%')`) = **0 rows**
  - 确认 tier 层 pexec_429/pexec_conn/pexec_500/pexec_noncycle_404/empty_200 全在 tier retry 内被吸收未上浮, 非新可配置类不需动

## fallback (cc4101 日志, 30min) — 负向核心指标
- cc4101 层 fallback **12** FALLBACK-OK (0 真中断, 0 fallback 失败)
- 11 条 = 11×75s `PRIMARY-FAIL-SKIP-CIRCUIT` (header/ttfb timeout 75s < chain budget 120s, cc4101 pre-empted
  nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层)
  - req 列表: f93b60c1(75068ms fb 22871ms) / 0478c237(75079ms fb 1885ms) / 944185ef(75009ms fb 10891ms) /
    ce5c3ac7(75081ms fb 12793ms) / 0cb60d0b(75041ms fb 9580ms) / df56399c(75083ms fb 7235ms) /
    53147bfb(75081ms fb 20191ms) + 4 条同模式 (全 75s SKIP-CIRCUIT, ms 救回)
  - 1 条不同模式: e366874e `[PRIMARY-FAIL] primary failed (unknown)` (非 75s timeout, unknown 失败) fb 2647ms 救回
  - **120s 跑满类连续第 4 轮 0 条** (R2039=0 → R2040=0 → R2042=0 → 本轮=0, 偏好持续回落)
- R2042 fallback 10 (全 10×75s SKIP-CIRCUIT) → 本轮 12 (+2 区间内波动, 120s 跑满类仍 0 条偏好持续)
- `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` cc4101 30min = **0** → 确认 0 真中断
- 全 12 条被 cc4101 抢断切 ms, ms 救回 → 0 条 fallback 失败 → CC 收 0 真 502

## nv_gw 层内部 ms_fb 路径 (新记录的兜底机制, 非恶化)
- 30min `all_keys_exhausted` 出现 27 次 (9 个 distinct req), 触发 nv_gw 内部 NV-MS-FB-ATTEMPT → NV-MS-FB-SERVED
  (nv_gw 自调 ms_gw 兜底, **不是 cc4101 层 fallback**, 是两条独立兜底路径)
- 30min NV-MS-FB-SERVED = **9** (6h = 77), 全部 `state=CLOSED` (breaker 未 OPEN 切流)
- 9 个 ms_fb req 在 nv_requests 表的状态: **8 个记 200 (ms_gw 救回成功)**, 1 个记 502 (f9ee281f
  zombie_empty_completion, ms_fb 也失败上浮)
  → 说明 nv_gw 层 ms_fb 成功率高 (8/9=88.9%), 救回的记 200 不算 502, nv_gw 内部兜底机制正常吸收
- cc4101 不记录 NV-MS-FB-SERVED (grep count=0) → 确认 cc4101 12 fallback 与 nv_gw 9 ms_fb 是独立路径
- 注: 这是 nv_gw all_keys_exhausted 后的自兜底 (R1719 breaker 仍 CLOSED count 2 未达 OPEN 阈值),
  **机制正常吸收非恶化**, 符合 CLAUDE.md "宁可走 ms 也不死循环" 的设计意图

## breaker
- cc4101 PRIMARY-BREAKER-OPEN 30min = **0**
- nv_gw 30min `grep -cE "BREAKER-FAIL|BREAKER.*OPEN|NV-ANTH-BREAKER-FAIL"` = **0**
  - R2042 nv_gw recorded 0 (R2040 req=ad2f46d8 已滚出窗口) → 本轮仍 0 (无新 NV-ANTH-BREAKER-FAIL 事件)
  - **state CLOSED count 仍 2 非 OPEN 切流 = 连续第 13 轮验证未恶化机制正常吸收非恶化**
  - nv_breaker recorded failure 计数仍 2 (未累积到 3), 仍 CLOSED 未达 OPEN 阈值

## BUG-A 修复 (R1913) 在日志中真实生效确认
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **9 次** (R2042 9→9 持平, 持续触发确认机制长期生效)

## nv_gw /health + StartedAt 核实
- /health = ok (proxy_role=passthrough, nv_num_keys=5, models=[kimi_nv,dsv4p_nv,glm5_2_nv],
  default=dsv4p_nv, port=40006)
- nv_gw StartedAt = **2026-07-20T03:02:10Z** (R2042 后未再漂移, **连续第 10 轮核实**, env 0 变更, 非 cc2 改)
- cc4101 StartedAt = 2026-07-19T12:10:22Z (0 restart 未变)
- docker ps nv_gw "Up 3 hours" 与 StartedAt 03:02:10Z UTC = 11:02:10 CST 到当前 14:51 CST (约 3.8h) 吻合

## 旁注: hermes2 (hm4104) 链路 breaker OPEN (非本链路, 仅记录)
- peer "hm2 hermes2" R2044 诊断: hm4104→hermes2 这条独立链路的 breaker OPEN (56 次 PRIMARY-BREAKER-SKIP,
  0 次 primary 成功), 根因是大 input(160k) 触发 nv_gw 60s stream_first_byte_deadline → hm4104 breaker 累积 OPEN
- **这是 hm4104→hermes2 链路, 不是我管的 cc4101→nv_gw 链路**, 不影响 cc2 本链路 (cc4101 breaker 30min=0)
- 但提示: 大 input 触发 nv_gw 60s stream_first_byte_deadline 是已知边界, 当前 cc2 链路 6h 502 中
  stream_first_byte_timeout×9 也属同类. 若未来 cc2 链路 stream_first_byte_timeout 持续增多逼近 fallback 失败,
  可考虑调 NVU_FORCE_STREAM_UPGRADE_TIMEOUT 或 NVU_FORCE_STREAM_EXCLUDE_MODELS (CLAUDE.md 可调旋钮). 当前不动.

## 决策: 继续 NOP (连续第 75 轮冻结)
- 数据全部满足"稳态":
  - 6h SR 95.40% > 94% 真实稳态 (vs R2042 -0.83pp 区间内偏移非退化, 仍稳态区间)
  - 30min SR 90.16% 略低但 61 req 小样本 + 502 全 NVCF 已知三类不可配
  - abs_cap 30min/6h 双确认归零
  - fallback 12 条全兜住 0 真中断 0 失败, 120s 跑满类连续 4 轮 0 条偏好
  - nv_gw 层 ms_fb 9 次 (all_keys_exhausted 触发) 全 CLOSED 非切流, 机制正常吸收
  - breaker 30min recorded 0 条 (连续第 13 轮验证未恶化)
  - BUG-A SKIP-PEXEC2 持续触发 9 次
- 冻结理由 (连续第 75 轮) 仍成立: 半成品未 in-vivo 验证 (env 开关从未激活) + 激活需同步
  chain_budget 120→420 + cc4101 header 60→450 + post-200 软挂换 key 未实现 + 24h 观测窗口,
  风险/收益不对等 (当前稳态, 边际收益小)

## 状态变化
- 无. nv_gw StartedAt 仍 03:02:10Z (连续第 10 轮核实), env 0 变更, 0 restart.
- 唯一需记录的变化:
  1. 6h SR 96.23%→95.40% (-0.83pp 区间内偏移, 仍在稳态区间, 非退化)
  2. tier 30min 新增 pexec_noncycle_404×10 (6h 502 上浮探测 0 rows, tier retry 吸收非新可配置类)
  3. 首次系统记录 nv_gw 层内部 ms_fb 路径 (all_keys_exhausted 触发, 30min 9 次/6h 77 次, 全 CLOSED)
     — 这是 nv_gw 自兜底机制正常工作, 非恶化, 之前 STATE 未单独记录此路径
