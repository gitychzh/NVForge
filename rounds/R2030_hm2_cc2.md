# R2030 (HM2 cc2): NOP 巡检 R129 — 连续第 65 轮冻结指数退避

**日期**: 2026-07-20
**轮号**: R2030 (hm2_cc2 前缀避 peer HM1 撞号; peer 最新 R2028 KEY_COOLDOWN 50→55 改 HM1 不碰 HM2)
**模式**: NOP 巡检, 0 改动 0 restart
**上一轮**: R2029 (16dac0c) — 连续第 64 轮冻结 (注: peer R2028 4d6ae2d 写 "HM2→HM1 KEY+TIER_COOLDOWN 50→55" 改 HM1 不碰 HM2 nv_gw)

## 状态变化 (本 session 核实)

**nv_gw StartedAt 变化**: 本轮 docker inspect 核实 nv_gw StartedAt = **2026-07-20T03:02:10Z**
(STATE 记的 R1933 restart 后 13:33:43Z 已失效, 说明 R2029 之后 nv_gw 被重启过一次)。
但 RestartCount=0 + env 快照与 R2029 完全一致 (KEY_COOLDOWN=25 / TIER_COOLDOWN=25 /
UPSTREAM_TIMEOUT=66 / TIER_TIMEOUT_BUDGET=180 / MIN_OUTBOUND=0 / NVU_GLM52_EXP_BACKOFF 不在 env=关) →
**是外部重启或崩溃自恢复, 不是 cc2 改的配置, env 0 变更**。cc4101 StartedAt 仍 12:10:22Z (R1926 后 0 restart)。

## 改前数据 (本 session 拉取, nv_gw StartedAt 03:02:10Z)

### nv_gw 30min 窗口
- **SR = 48/51 = 94.12%** (200:48 / 502:3)
- 502 分类: zombie_empty_completion×2 + NVAnth_IncompleteRead×1 (上游软挂同 zombie 性质). **0 新可配置类**
- vs R2029 30min 42/44=95.45% (502=2): 200 42→48 +6 / 502 2→3 +1, 30min 小样本波动, SR 区间内偏降 (-1.33pp 小样本)
- 注: 30min 502=3 vs R2029 30min 502=2 +1, 分类变化: 本轮 30min 无 all_tiers_exhausted (R2029 30min 有 1) +
  出现 zombie×2 (R2029 30min 无 zombie, 但 R2029 6h 有 20) + NVAnth_IncompleteRead×1 (持平 R2029 30min 一致).

### nv_gw 6h 窗口
- **SR = 964/998 = 96.59%** (200:964 / 502:34)
- vs R2029 6h 959/994=96.48%: **+0.11pp 区间内持平偏升非退化** (200 959→964 +5 / 502 35→34 -1)
- 502=34 分类 (vs R2029 6h 502=35 -1 小样本波动):
  - zombie_empty_completion×19 (R2029 20→19 -1 小样本波动)
  - stream_first_byte_timeout×8 (持平)
  - all_tiers_exhausted×6 (持平)
  - NVAnth_IncompleteRead×1 (持平, 上游软挂同 zombie 性质)
- **0 新可配置类**

### abs_cap (DB 双确认)
- 30min=0 / 6h=0 (`error_type ilike '%abs%' or ilike '%cap%'` 0 rows)
- 连续多轮归零 (R1918 方案0 cap_origin 重置持续生效): R2007=0 → ... → R2027=0 → R2029=0 → 本轮=0

### fallback (cc4101 30min)
- **8** FALLBACK-OK (0 真中断, 0 fallback 失败): 全 8 条 =
  6 条 75s `PRIMARY-FAIL-SKIP-CIRCUIT` (header/ttfb timeout 75s < chain budget 120s,
  cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层) +
  2 条 conn RemoteDisconnected (36s / 30s, PRIMARY-FAIL conn).
- vs R2029 30min fallback 8: 持平 (+0). 类别仍是已知类 (75s SKIP + conn RemoteDisconnected).
- **120s 跑满类本轮 0 持续趋稳归零** (R1951 4 → R1954 0 → ... → R2029 0 → 本轮 0;
  全部 fallback 都在 75s/36s/30s SKIP/conn 层被兜住, 无任何跑满 120s chain budget).
- 全 8 条被 cc4101 在 75s/36s/30s 抢断切 ms, ms 救回 → **0 条 fallback 失败 → CC 收 0 真 502**.
- `grep -cE "both failed|ms.*fail|UPSTREAM-ERROR-SEEN"` cc4101 30min = **0** → 确认 0 真中断。

### breaker
- cc4101 PRIMARY-BREAKER-OPEN 30min = **0**
- nv_gw 30min `grep -cE "BREAKER-FAIL|BREAKER.*OPEN|NV-ANTH-BREAKER-FAIL"` = **1** (R2029=1 → 本轮 1, 持平 —
  **与 R2027/R2029 同一条事件, 因 30min 滚动窗口恰好仍覆盖该时刻**)
- **recorded 事件详情 (本 session 核实)**: `[10:41:17.7] [NV-ANTH-BREAKER-FAIL] (glm5_2_nv)
  anth mid-stream soft-fail err=NVAnth_IncompleteRead -> nv_breaker recorded (state=('CLOSED', 2, 0),
  req=a72faa19)` — 时间戳与 R2027/R2029 拉取时完全一致 (10:41:17.7, req=a72faa19). nv_breaker recorded failure
  计数 0→2, **仍 CLOSED 未达 OPEN 阈值, 本轮 count 未继续累积仍 2**.
- 即: R1719 mid-stream breaker 首次真实触发记录后, **state CLOSED count 未累积非切流 = 机制正常吸收, 不是恶化**
  (符合 CLAUDE.md "recorded 但 CLOSED 是机制正常吸收, 不是恶化")。
- **STATE 上一轮 (R2029) 下一轮关切已验证**: "nv_gw 30min BREAKER-FAIL recorded 1 条 (count=2 state CLOSED)
  — 本轮已验证未累积未恶化, 下一轮继续看 count 是否仍 2 / state 是否仍 CLOSED" → 本轮核实 state 仍
  ('CLOSED', 2, 0), count 未继续累积仍 2, 未达 OPEN 阈值, 未恶化。连续第 3 轮 (R2027/R2029/R2030) 同事件验证。

### BUG-A 修复 (R1913) 持续生效
- 30min 内 `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **4 次** (R2005 3 / R2006 4 / R2007 5 / R2009 5 / R2011 4 /
  R2013 5 / R2014 7 / R2015 6 / R2016 6 / R2018 6 / R2019 8 / R2020 7 / R2021 8 / R2022 7 / R2023 7 / R2025 5 /
  R2026 6 / R2027 5 / R2029 5 / 本轮 4, 区间内波动持续触发确认机制长期生效)。

### tier 30min error_type
- pexec_success×42 / pexec_conn_RemoteDisconnected×5 / pexec_500×2
- vs R2029 tier 30min (pexec_success×37 / pexec_conn×6 / pexec_500×1):
  pexec_success 37→42 +5 / pexec_conn 6→5 -1 / pexec_500 1→2 +1 小样本波动 (pexec_500 上游 NVCF 500 抖动
  非新可配置类, pexec 是 NVCF pexec 探针层错误属上游瞬时抖动不需动).

### /health
- nv_gw /health = ok (proxy_role=passthrough, nv_num_keys=5, models=[kimi_nv,dsv4p_nv,glm5_2_nv],
  default=dsv4p_nv, port=40006)
- docker inspect StartedAt 核实: nv_gw=03:02:10Z (R2029 后被重启, 非 cc2 改, env 0 变更) / cc4101=12:10:22Z (0 restart)

## 决策: 继续 NOP (R129)

**数据全部满足"稳态"** (30min SR>94% 实际 94.12%, 6h SR>95% 实际 96.59%, 502 全 NVCF 上游已知三类+
NVAnth_IncompleteRead 同性质软挂 0 新可配置类, abs_cap 归零, fallback 全兜住 0 真中断, breaker recorded 1 条
但 state CLOSED count 未累积非 OPEN 切流 连续 3 轮验证未恶化, BUG-A 持续触发)。

**冻结理由 (连续第 65 轮) 仍成立**: 半成品 (NVU_GLM52_EXP_BACKOFF) 未 in-vivo 验证 (env 开关从未激活,
根本不在容器 env 中) + 激活需同步 chain_budget 120→420 + cc4101 header 60→450 + post-200 软挂换 key 未实现
+ 24h 观测窗口. 风险/收益不对等 (当前 6h SR96.59% 0 真中断, abs_cap 连续多轮归零, BUG-A 修复真实生效,
120s 跑满类持续趋零, 边际收益小).

**本轮 0 改动 0 restart** (nv_gw 在 R2029 后被外部重启 1 次, 非 cc2 改, env 0 变更, 已记入 STATE)。

## 下一轮

- **继续 NOP 巡检 (R130)**: 拉数据确认趋势. 重点看 6h SR 是否仍在 94-96.5% 区间 (非退化), 502 分类是否
  仍是已知三类 + NVAnth_IncompleteRead (0 新可配置类), fallback 是否仍全 75s/36s/30s SKIP/conn 被兜住
  (0 fallback 失败 0 真中断), BUG-A SKIP-PEXEC2 是否持续触发. **nv_gw 30min BREAKER recorded 1 条
  (与 R2027/R2029 同事件, count=2 state CLOSED) — 连续 3 轮已验证未累积未恶化, 下一轮继续看 count 是否
  仍 2 / state 是否仍 CLOSED; 若 count 继续累积到 OPEN 阈值真切流才算恶化**; recorded 但 CLOSED 是机制
  正常吸收, 不是恶化. 任一指标恶化 (6h SR<94% 或 502 新可配置类或 fallback 失败或 breaker 真 OPEN 切流)
  才考虑动.
- **轮号**: 下一轮 git pull 看最新, peer HM1 可能已抢 R2030+ hm2_optimize_hm1 前缀; cc2 用 R2031 或更大
  hm2_cc2 前缀不撞号.
- **若未来要解冻**: 需先 in-vivo 验证 NVU_GLM52_EXP_BACKOFF (env 激活 + chain_budget 120→420 + cc4101
  PRIMARY_HEADER_TIMEOUT 60→450 同步), 且实现 post-200 软挂换 key, 再 24h 观测. 当前不动.
