# R369: HM1→HM2 — ⏸️ NOP · CC清单HM2-A/B/C三项独立复现: A(2.5已做+零429)/B(6h per-key均匀k3不离群证伪)/C(24h有25个90-128s慢成功→再降BUDGET误杀,不可行) · 30min 137/138=99.28% · 零429/empty200 · 容器env与live compose双处零漂移 · 铁律:只改HM2不改HM1(零配置变更)

**轮次**: HM1 优化 HM2 (HM1=执行者, HM2=反对者)
**角色**: HM1=执行者, HM2=反对者
**日期**: 2026-06-30 15:14 UTC+08 (CST)
**触发**: HM2新commit 6df4585 (R368末尾"轮到HM1优化HM2"标记触发)
**作者**: opc_uname (HM1)
**铁律**: 只改HM2不改HM1 ✅ (本轮零配置变更)

---

## 📊 数据采集 (HM2 30min+6h+24h实时窗口, host_machine='opc2sname')

### 时区确认 (R320教训#5)
本轮观测: 对端DB `NOW()=2026-06-30 07:14 UTC`, 但 `max(ts)=2026-06-30 15:13` (=当前CST 15:14)。即 hm_requests.ts 存的是 **CST 字面时间**(非UTC), 与R367记录不同(对端容器/DB TZ可能变化)。
按"绝对时间锚点"原则, 全部查询用 CST 字面时间戳 `ts >= '2026-06-30 14:43:00'`, 禁止 NOW()-interval。锚点基于实测 max(ts)=15:13 CST, 数据新鲜。

### 改前30min总览 (CST 14:43-15:13)
| span_min | total | ok | failed | pct | avg_ms | p50 | p95 | max_ms |
|----------|-------|-----|--------|-----|--------|-----|-----|--------|
| 30 | 138 | 137 | 1 | 99.28 | 10028 | 7154 | 24376 | 92145 |

**成功率 137/138 = 99.28%**。唯一失败 = 1个 all_tiers_exhausted (idx=NULL聚合, 92145ms, 耗近BUDGET=100s)。零429/零empty200/零NVStream。吞吐≈4.60 req/min (138/30)。

### 改前30min per-key (HM2-B独立复现: 验证无劣化key)
| key(idx) | reqs | avg_ms | p50 | p95 | max_ms | fails |
|----------|------|--------|-----|-----|--------|-------|
| k0(idx0) | 24 | 8512 | 7644 | 15823 | 21345 | 0 |
| k1(idx1) | 32 | 8010 | 5892 | 16325 | 35835 | 0 |
| k2(idx2) | 27 | 7670 | 5423 | 21257 | 23005 | 0 |
| k3(idx3) | 29 | 13784 | 5853 | 44364 | 90057 | 0 |
| k4(idx4) | 25 | 8969 | 8816 | 15692 | 18577 | 0 |

**30min窗口k3离群**: k3 avg=13784ms/p95=44364ms/max=90057ms, 远高于其他key(avg 7670-8969ms/p95 15692-21257ms)。看似HM2-B的劣化key信号。但需更长窗口复核(见下6h表)。

### 6h per-key (HM2-B核心复核: k3是否稳定病态, ts>=09:13 CST)
| key(idx) | reqs | avg_ms | p50 | p95 | max_ms | fails | slow_pct(>25s) |
|----------|------|--------|-----|-----|--------|-------|---------------|
| k0(idx0) | 264 | 10174 | 6961 | 31542 | 62346 | 1 | 6.8% |
| k1(idx1) | 360 | 10617 | 6537 | 35850 | 84213 | 0 | 8.3% |
| k2(idx2) | 309 | 9452 | 6128 | 32037 | 64733 | 0 | 6.1% |
| k3(idx3) | 305 | 9162 | 5808 | 31089 | 90057 | 0 | 6.6% |
| k4(idx4) | 296 | 10196 | 7378 | 29272 | 67612 | 0 | 6.1% |

**6h窗口k3完全不离群**: k3 avg=9162ms(5key中最低)、p95=31089ms(中游)、slow_pct=6.6%(中游)。30min的k3离群(avg13784/p95=44364/max=90057)是**随机NVCF上游慢响应抖动**, 非稳定病态。
对比HM1-R366观测到的k4-direct病态(avg28.5s/p95=72.9s/max=162.9s, 持续离群): HM2无同型病态key。**HM2-B独立证伪**, 与R360/R367结论一致。

### HM2-C核心复核: 24h有无90-128s区间成功(BUDGET可否再降, R367反对者预案精确补采)
R367反对者预案: "若认为HM2-C BUDGET可再降, 需先查HM2 24h有无100-128s区间成功(本轮未单独查询)"。本轮精确补采:

| 区间 | 成功请求数 |
|------|-----------|
| <50s | 3379 |
| 50-90s | 106 |
| **90-100s** | **6** |
| **>100s** | **19** |
| **90-128s合计** | **25** |

**24h有25个成功请求落在90-128s区间**(6个在90-100s, 19个在>100s)。若BUDGET从100再降:
- 降→90s: 误杀6个90-100s成功 + 影响19个>100s(本已超100s会被ATE, 但其中部分可能在新BUDGET内被截断)
- 成功率损失: 25/3534 ≈ 0.71% 成功转失败
- 节省: 失败请求早结束, 但HM2当前24h ATE仅102个avg=116.7s, 再降收益有限

**HM2-C再降不可行**: 误杀25个慢成功的成本 > 早结束失败的收益。R334的BUDGET=100已是平衡点, 不可再降。本轮把R367"未单独查询"补全为具体数据, HM2-C从"R334闭环"升级为"再降误杀25慢成功→不可行"。

### 24h ATE结构 (闭环确认)
| ate | avg_ms | p50 | max_ms |
|-----|--------|-----|--------|
| 102 | 116472 | 122120 | 128337 |

ATE 102个 avg=116.7s 耗近BUDGET=100s(注: p50=122s/max=128s略超100s, 因BUDGET是tier级预算含retry切换开销, 非硬截断), 属NVCF上游不可达时段预期行为, 非配置可消除。

---

## 🔧 CC定向清单HM2节三项状态 (本轮独立复现)

| 项 | 状态 | 证据 |
|----|------|------|
| **HM2-A** MIN_OUTBOUND 4.5→2.5 | ✅ R327已做 | 容器env=2.5 + live compose `/opt/cc-infra/docker-compose.yml` hm40006段 `MIN_OUTBOUND_INTERVAL_S: "2.5"  # R327` + 30min零429闭环 |
| **HM2-B** 劣化key补采 | ✅ 本轮独立复现证伪 | 30min k3看似离群(avg13784/p95=44364), 但6h窗口k3完全不离群(avg9162最低/p95=31089中游/slow_pct6.6%中游) → 随机抖动非病态 |
| **HM2-C** BUDGET 128→100 | ✅ R334已做+本轮再降证伪 | R334已做env=100; 本轮补采24h有25个90-128s慢成功(6个90-100s+19个>100s) → 再降误杀25慢成功不可行 |

三项均已做完或被独立数据证伪。按CC规则"不允许无操作轮除非三项都已做完或数据证伪"——本轮属合规无操作 (证伪/再降不可行数据见上)。

---

## 🔍 配置漂移核对 (R322教训#1/#2: live compose必须在场)

### 容器运行态 env (docker exec hm40006 env)
```
MIN_OUTBOUND_INTERVAL_S=2.5        (HM2-A 已做)
TIER_TIMEOUT_BUDGET_S=100          (HM2-C 已做)
TIER_COOLDOWN_S=22
UPSTREAM_TIMEOUT=50
KEY_COOLDOWN_S=38
HM_CONNECT_RESERVE_S=21
HM_SSLEOF_RETRY_DELAY_S=1.0
HM_NV_PROXY_URL1=http://host.docker.internal:7894
HM_NV_PROXY_URL2= (空, direct)
HM_NV_PROXY_URL3= (空, direct)
HM_NV_PROXY_URL4= (空, direct)
HM_NV_PROXY_URL5=http://host.docker.internal:7899
```

### live compose (/opt/cc-infra/docker-compose.yml, hm40006服务段)
```
MIN_OUTBOUND_INTERVAL_S: "2.5"      # R327  ← 与容器一致
TIER_TIMEOUT_BUDGET_S: "100"        # R334  ← 与容器一致
```
(注: compose中另有其他服务段MIN_OUTBOUND=1.5, 非hm40006段, 不影响)

### health确认
`curl /health` → `{"status":"ok","proxy_role":"passthrough","hm_num_keys":5,"nvcf_pexec_models":["glm5.1_hm_nv"],"hm_default_model":"glm5.1_hm_nv"}` — HM2后端=glm5.1_hm_nv(不可改)确认, 5 key, 健康运行。

**零漂移**: 容器运行态 = live compose 关键参数一致。R322教训#1已防。

### live compose不在git (R322教训#2)
`/opt/cc-infra/docker-compose.yml` 是live文件, 不在git仓库内。本轮零配置变更, 无同步需求, 此处仅漂移核对留证。

---

## ✅ 决策: ⏸️ NOP (No Operation)

**原因**: CC定向清单HM2节三项 (HM2-A/B/C) 本轮独立采30min+6h+24h实时数据全部复现:
- **HM2-A** (MIN_OUTBOUND=2.5): R327已做, env+compose双处一致, 30min零429闭环
- **HM2-B** (劣化key): 30min k3看似离群(avg13784/p95=44364), 但6h窗口k3完全不离群(avg9162最低/p95=31089中游/slow_pct6.6%中游) → 随机抖动非病态, 独立证伪
- **HM2-C** (BUDGET=100): R334已做; **本轮补采R367反对者预案留的"未单独查询"**: 24h有25个90-128s慢成功(6个90-100s+19个>100s), 再降BUDGET误杀25慢成功不可行

30min 137/138=99.28% (唯一失败1个ATE耗92145ms属NVCF上游不可达), 零429/零empty200。配置零漂移。全参数达天花板, 无可优化空间。

**本轮核心增量**: 把R367反对者预案"HM2-C BUDGET可再降需先查24h有无100-128s区间成功(本轮未单独查询)"补全为精确数据(25个慢成功), 从而把HM2-C从"R334闭环"升级为"再降误杀25慢成功→不可行"。这是本轮非空的数据贡献, 非简单重复R367。

**连续NOP轮数**: 第18轮 (HM1→HM2链持续)

**铁律**: 只改HM2不改HM1 (零配置变更) ✅

**参数变更**: 无

**反对者预案**: 下轮HM2若认为HM2-B的k3 30min离群值得改路由, 需先采6h+窗口复核(本轮6h已证k3不离群); 若认为HM2-C BUDGET仍可降, 需解释如何避免误杀24h内25个90-128s慢成功(6个在90-100s, 19个>100s)。

## ⏳ 轮到HM2优化HM1
