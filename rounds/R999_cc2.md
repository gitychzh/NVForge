# R999 cc2 NOP 巡检轮

日期: 2026-08-07 15:10 CST
状态: **NOP 不改码** — cc2 主链路连续第 **107** 轮 100% 干净

## 轮前链路数据 (注入 + live 复核 2026-08-07)

- 30min cc4101-primary (主 nv_gw:40006) = **119/119 全 200 = 100% SR, 0 bad** (live re-pull)
- 主链专属错误 (caller=cc4101-primary, status!=200) = **0 rows**
- 本轮 window 非 200 总 bad = 5 条, **经 caller 归属 JOIN 判定全属 hermes** (dsv4f0731_nv 502:
  2 all_tiers_exhausted + 2 zombie_empty_completion + 1 stream_absolute_cap), 非 cc2 主链
- 30min fallback (cc_requests) = **0 次** (121 total, 全未 fallback_triggered, 0.0%)
- nv_tier_attempts: 非 pexec_success 全属 NVCFPexecRemoteDisconnected / NVCFPexecTimeout / empty_200
  瞬态 (round-robin + func_health 吸收), 5 key 全有 pexec_success, 无持久 key 疲劳
- buffer 日志: 全 attempt=1 一次成功 (elapsed 8-10s, flush 1.9-10KB), 无 WAIT 停滞, 无多 attempt 泄漏
- ms_gw fallback 保持不禁用 (NVU_DISABLE_MS_FALLBACK=0)

## 本轮改动

**无** (NOP。主链 119/119 100% SR + 专属错误 0 rows + fallback 0 次, 无根因可查, 无参数可调)

## 当前配置快照

- nv_gw: NVU_DISABLE_MS_FALLBACK=0, NVU_BUFFER_MAX_RETRIES=5, KEY_COOLDOWN_S=30,
  TIER_COOLDOWN_S=180, TIER_TIMEOUT_BUDGET_S=180, UPSTREAM_TIMEOUT=90, NV_INTEGRATE_KEY_COOLDOWN_S=90,
  NVU_BUFFER_CALLERS=cc4101-primary,openclaw2, NVU_CALLER_KEY_MAP=hermes:2;openclaw:3;opencode:4
- cc4101: PRIMARY_UPSTREAM_MODEL=dsv4f0731_nv, FALLBACK_UPSTREAM_MODEL=glm5_2_ms,
  CC4101_STREAM_TOTAL_DEADLINE_S=470, PRIMARY_HEADER_TIMEOUT=400, CC4101_PRIMARY_FAIL_THRESHOLD=3

## 关键判断

- cc2 主链路连续第 **107** 轮 (R893-R999) 100% SR 干净, 主链专属错误 0 rows。
- 本轮 window 5 条 bad (dsv4f0731_nv 502) 经 caller JOIN 判定全属 hermes 越界宿主 →
  与主链 host 分离保持, 属已知 bad-fid 52e1ddb6 泄漏, 非 cc2 优化范围。
- fallback 0 次, 无新 cc2 主链错误类, 无持久 key 疲劳。
- buffer 一次成功 + multi-key round-robin + func_health 完全吸收瞬态键错误, 稳态维持。
- **不改码**: ①主链 SR 100% + 专属错误 0, 无优化需求; ②bad 全属 hermes 越界, 主链无根因; ③链路已稳态。

## 下一步

- 保持 NOP 观察, 主链 dsv4f0731_nv 为首代, 无需参数改动。
- 继续确认 hermes 越界 bad (fid 52e1ddb6) 与主链 host 隔离保持。