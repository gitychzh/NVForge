# R1994 (HM2 cc2) — NOP 巡检 R102, 连续第 39 轮冻结指数退避

> 0 改动, 0 restart. 延续 R1928 冻结决定 (半成品指数退避未 in-vivo 激活, env NVU_GLM52_EXP_BACKOFF 根本不在容器 env 中).
> 本 session 全新, 无上一轮对话上下文, 依 STATE.md + 本轮自拉数据决策.
> 上一轮 R1993 (commit 8771a85) 为 peer HM1 agent 写的 hm2_optimize_hm1 前缀轮, 与 cc2 前缀不撞号.
> nev_gw 真实 StartedAt = 2026-07-19T13:33:43Z (R1933 restart 后未再 restart, docker inspect 核实 R1933→R1994 维稳).

## 数据 (本 session 拉取, 30min + 6h 双窗口)

- **30min nv_gw SR = 134/138 = 97.10%** (200:134 / 502:4). 小样本偏稳态.
  趋势对照: R1992 30min 97.16 / R1991 96.72 / R1990 95.97 / R1989 95.87 — 区间稳态非退化.
- **6h nv_gw SR = 887/924 = 95.99%** (200:887 / 502:37). 大样本稳态区间.
  趋势对照: R1992 6h 95.99% (861/897) → R1994 6h 95.99% (887/924). **0 漂移, 完全持平**.
- **30min 502=4 全 NVCF 上游侧已知类**: stream_first_byte_timeout×2 + all_tiers_exhausted×1 + zombie_empty_completion×1
  (全 glm5_2_nv, 出口 IP 段 134.195.101.0/24 同源快回空).
  与 R1992 30min 502 分类 (fbt×2 + ATE×1 + zombie×1) **完全一致, 0 新类 0 重新分布**.
- **6h 502=37 全已知类**: zombie×25 + fbt×7 + ATE×5.
  与 R1992 6h 502=36 (zombie×25 + fbt×6 + ATE×5) 对比: **zombie 持平 25, ATE 持平 5, fbt +1** (6h 窗口随机波动, 0 新可配置类).
- **abs_cap 30min=0 / 6h=0** (DB `error_type like '%abs%'` 双窗口 0 rows 双重确认).
  日志中 NV-PEEK-CAP-RESET / NV-CAP-RESET-MSFB 30min×6 是 R1918 方案0 cap_origin reset 事件, 非真 abs_cap 502.
  连续多轮归零链: R1946=0 → ... → R1992=0 → R1994=0.
- **fallback 6 FALLBACK-OK (0 真中断, 0 fallback 失败)**:
  全 6 条 75s `PRIMARY-FAIL-SKIP-CIRCUIT` (< chain budget 120s, cc4101 pre-empted nv_gw retry, NOT counted toward circuit — R1947 已知类 cc4101 bug3 preempt 层).
  趋势: R1967 8/R1970 7/.../R1990 3/R1991 4/R1992 5/R1994 6 — 区间内波动, 无恶化.
  全 6 条被 cc4101 在 75s 抢断切 ms, ms 2476/3807/4013/3485/1909ms 救回 → **0 条 fallback 失败 → CC 收 0 真 502**.
  `grep -cE "both failed|UPSTREAM-ERROR-SEEN"` = 0 → 0 真中断确认.
- **breaker**: cc4101 PRIMARY-BREAKER-OPEN 30min = 0; nv_gw 30min MS-FB-SERVED 6 条全 `state=CLOSED`
  (recorded failure 未达 5/300s 阈值仍 CLOSED, 非 NV-ANTH-BREAKER-FAIL); `grep -cE "BREAKER-FAIL|BREAKER.*OPEN"` = 0. breaker **OPEN 0 连续多轮**.
- **BUG-A 修复 (R1913) 真实生效**: 30min `NV-GLM52-CHAIN-SKIP-PEXEC2` 触发 **6 次**, skip _try_tier_keys 第二轮省约 ~120s/fallback 请求.
  趋势: R1952 6/R1970 4/.../R1990 2/R1991 3/R1992 4/R1994 6 — 区间内波动持续触发, 长期生效.

## 决策: NOP 巡检轮 R102

- 数据全部满足"稳态": 30min SR>95%, 6h SR>95%, 502 全 NVCF 上游已知类, abs_cap 归零, fallback 全兜住 0 真中断, breaker 0 OPEN, BUG-A 持续触发.
- **冻结理由 (连续第 39 轮仍成立)**: 半成品未经 in-vivo 验证 (env NVU_GLM52_EXP_BACKOFF 不在容器 env) + 激活需同步 chain_budget 120→420 + cc4101 header 60→450 + post-200 软挂换 key 未实现 + 24h 观测窗口. 风险/收益不对等.
- 依用户诉求 "可以报错但不能让 cc2 中断" 仍达成 (R1994 0 真中断, 6 条 FALLBACK-OK 全被 ms_gw 兜住).
- 不改代码, 不 restart. 继续观测.

## 验证

- 本轮 0 改动, 0 restart. nv_gw StartedAt 维 R1933 (13:33:43Z), 未变.
- 数据自洽: 30min + 6h 双窗口一致, 502 分类与 R1992 一致 (0 新类), fallback 全被兜住.
